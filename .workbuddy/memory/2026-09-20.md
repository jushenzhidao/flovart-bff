
## Chatfire 异步生图接口调试（2026-09-20）
- 文档：POST /async/v1/images/generations（202→task_id/status=QUEUED）+ GET /async/v1/images/generations/{task_id}。基座 api.chatfire.cn。
- 经 workbuddy 网关（newapi-bff）实测，令牌 sk-KfTV...ht7D + New-Api-User:6：
  - **POST /async/* 是盲透传**：假模型也 202（创建成功≠模型可用），不做模型/渠道校验
  - **GET 查询走 distributor 按 model 找渠道**：模型不在用户分组的启用渠道里 → 503 model_not_found
  - gpt-image-2 全链路通：202 QUEUED → 202 IN_PROGRESS → 200 出图
  - **结果形态按模型不同**：doubao-seedream → data[].url（火山 TOS 签名 URL，86400s 过期）；gpt-image → data[].b64_json + revised_prompt
  - 任务态字段：task_id/status（QUEUED/IN_PROGRESS/终态待确认）/created_at/scheduled_at/batch_key/batch_state/replayed
- 遗留：doubao-seedream-5-0-pro-260628 不在 group default 任何渠道 → 飞哥需在面板把该模型加进 chatfire 渠道（image-2）后查询才通
- 接入方案待飞哥拍板：flovart-bff tasks.py 加 async 提交+轮询模式 vs model-plaza 侧接入

## Chatfire 异步生图接入 flovart（2026-09-20，已完成代码+测试）
- 飞哥拍板接入 flovart 画布生图。
- **config.py**：新增 `GATEWAY_ASYNC_IMAGE_SUBMIT_PATH`（默认 async/v1/images/generations）+ `GATEWAY_ASYNC_IMAGE_MODELS`（前缀匹配，默认 doubao-seedream）。
- **tasks.py**：`_is_chatfire_async_image_model`（前缀判定）+ `_submit_chatfire_async_image`（POST 裸 {model,prompt,image[]}，provider 记 **gateway-async-image**，size 等扩展参数不外发）+ `_poll_chatfire_async_image`（**data 键出现即终态**——完成响应无 status 字段；error 体/FAILED 系→failed 统一包 {"error":...}；空 data→无产物失败不记 succeeded）+ `_CHATFIRE_ASYNC_FAILED` 常量。
- submit() async 分支前置 chatfire 判定（kind=image-gen + 前缀命中 + mode=async）；get_task() 按 provider=gateway-async-image 分流。
- 踩坑：Edit 换函数时把 `_is_gemini_image_model` 的 def 行吞了（docstring 悬空），靠 pytest import 报错前自查 Read 发现修复。
- 测试：tests/test_chatfire_async_image.py 18 个，全套 **127 passed**。
- 前端零改动：mode=async 由 AI 服务配置 params.mode 带来（既有机制）。
- 待办：①飞哥面板把 doubao-seedream-5-0-pro-260628 挂到 chatfire 渠道（default 分组）→ 查询才通；②前端建服务配 mode=async；③BFF 出新镜像部署（连同 gemini 路由一起）；④两个已创建的 doubao 任务 task_id（doubao_seedream_00de27b789.../b22e68c5ff...）留着渠道修好后可复测查询。

## 管理员 PAT 互踢根治（2026-09-20，flovart-bff + hewapi 双侧落地）
- 痛点：两 BFF 共用 uid=1 + 同 PAT 连同一台 new-api。旧逻辑 401 → 盲目 `_admin_login()`（GET /api/user/token 轮换 PAT）→ 互作废对方 → 乒乓 → 50 会话/100 签发双爆 → 「登录设备数已达上限」死锁（new-api 会话永不过期）。
- 根治三件套（两仓库同构实现）：
  1. `_reload_admin_cred()`：401 先重读共享凭据文件，对端已轮换落盘就直接采纳，绝不 login；
  2. `_self_heal_admin_cred()`：文件无新值 → 锁内双检（①缓存 PAT 已变=同进程治愈 ②重读文件=跨进程治愈）→ 仍无效才 login，单飞锁防并发双轮换；`_save_admin_cred(force=True)` 强制落盘（env 直供也写）；
  3. `NEWAPI_ADMIN_LOGIN_FALLBACK=0` 可禁自动轮换转人工。
- 部署：两边 compose 共挂外部卷 `flovart-admin-cred` → /shared，`BFF_ADMIN_CRED_FILE=/shared/admin_cred.json`（flovart 负责 name+create，hewapi external:true 引用；先起 flovart 或手动 docker volume create）。flovart 的 BFF_ADMIN_CRED_FILE 从 /data/admin_cred.json 改到 /shared —— **旧缓存 PAT 作废需重新登录一次落盘**。
- 测试：flovart tests/test_admin_cred_selfheal.py 11 个（全量 138 passed）；hewapi 同款 11 个（全量 5 个失败为基线遗留：static/docs.js ✓ emoji 门禁 + docs 档案目录，与本次无关）。
- 应急脚本同步到 flovart：scripts/clear_sessions.py（auth_version 抬升吊销全部会话解死锁，⚠️ 连带作废该账号 PAT，用后必须重登换新）+ fix_session_limit.py。
- ⚠️ 面板「生成令牌」仍会作废 PAT：现在两边会经共享文件自愈（第一个 401 触发轮换、第二个 401 读文件），最多一轮收敛，不再乒乓。
- 飞哥本地 newapi-bff 不在 git 仓库内（.git 缺失），改动无法 commit——提醒过。

## ⚠️ 修正：两 BFF 不在同一台服务器（飞哥 2026-09-20 指出）→ 方案改为「读回恢复」
- 共享卷方案作废（Docker volume 跨不了机器，hewapi 侧 external 引用还会起不来）。两份 compose 已回退：各自用本地 /data/admin_cred.json 缓存，删除 flovart-admin-cred 卷。
- **新核心：读回恢复 `_recover_admin_pat_by_readback()`**：401 自愈序列 = ①重读本地凭据文件（同机多实例用）→ ②锁内双检 → ③**login 拿会话后 GET /api/user/self 把账号当前 access_token 原样读回来**（读操作不轮换不作废，对端服务器零感知）→ ④读回为空（账号没设过 token）才最后走轮换兜底。开关 `NEWAPI_ADMIN_PAT_READBACK`（默认 1）。
- 登录逻辑重构：抽出 `_admin_session_login()`（只建会话不轮换，用完必归还）；`_admin_login()` 仅剩轮换兜底职责。读回的 PAT 同样强制落盘。
- 测试：两边 test_admin_cred_selfheal.py 各 15 个（+4 读回用例：成功不轮换/空回落轮换/异常回落/会话归还）。flovart 全量 **142 passed**；hewapi 135 passed + 5 个基线遗留失败（docs.js emoji 门禁，与本次无关）。
- 部署注意：两台服务器各自出镜像重启即可，无共享卷/无部署顺序要求。旧 admin_cred.json 缓存里的 PAT 若已失效，首个 401 会触发读回自愈。
- 风险备注：`/api/user/self` 是否返回 access_token 字段未经线上实证（验证需登录，铁律禁自动试）。若某版本把它抹掉 → 读回恒空 → 自动降级为轮换兜底（安全但退化回互踢风险），部署后看日志有无 "recovered by readback" 即知。

## compose 终态确认（2026-09-20 10:47）
- 飞哥问「除镜像外 compose 要不要改」→ 复查发现 **flovart compose 残留** `BFF_ADMIN_CRED_FILE: /shared/admin_cred.json`（上一轮回退时 volumes 挂载和卷定义删了、但这行 env 没改成——/shared 不再挂载，read_only 容器里写凭据必失败）。已修为 `/data/admin_cred.json`。
- 终态：两边 compose 均为本地 /data/admin_cred.json、无共享卷、无 /shared，语法校验过。**服务器上 compose 文件需要同步更新（用本地这份），不是不用改**——准确说法：不用手工编辑，直接上传替换本地最新版即可。
- 教训：多段回退后要 grep 全量验证残留，别只看最后一段编辑成功。

## hewapi 重新拉取代码后重打补丁（2026-09-20 11:02）
- 飞哥 git 文件丢失、重新 clone（HEAD=36a2835，工作区干净）→ 我们的全部改动消失。
- 重新应用全套（新版代码结构与旧版一致，补丁一次命中）：newapi_client.py（+asyncio import、_save_admin_cred force、_reload_admin_cred、单飞锁、_self_heal_admin_cred、_admin_session_login、_recover_admin_pat_by_readback、_admin_login 重构、admin_request 401→自愈）+ config.py（READBACK/LOGIN_FALLBACK 开关）+ compose（BFF_ADMIN_CRED_FILE=/data/admin_cred.json）。
- 重建 tests/test_admin_cred_selfheal.py（15 个，request mock 用 client_ip 签名适配新版）。
- 踩坑：hewapi 有 emoji 门禁（test_no_emoji 扫 app/*.py），补丁注释里的 ⚠️（U+26A0）撞门禁 → 已全部换文字，码点级验证清零。**以后给 hewapi 写注释禁用一切 emoji/符号字符**。另一失败 test_checker_detects_emoji_in_frontend 为基线遗留（static/docs.js）。
- compose config 校验：新克隆无 .env，插值变量（APP_VERSION/VCS_REF/BFF_SECRET_KEY/NEWAPI_*/ADMIN 账密）全 required —— 服务器上有 .env 不受影响；本地校验需临时 env 假值。终态 3 文件 +161/-14 + 新测试。

## hewapi 重新拉取代码后重打补丁（2026-09-20 11:02）
- 飞哥 git 文件丢失、重新 clone（HEAD=36a2835，工作区干净）→ 我们的全部改动消失。
- 重新应用全套（新版代码结构与旧版一致，补丁一次命中）：newapi_client.py（+asyncio import、_save_admin_cred force、_reload_admin_cred、单飞锁、_self_heal_admin_cred、_admin_session_login、_recover_admin_pat_by_readback、_admin_login 重构、admin_request 401→自愈）+ config.py（READBACK/LOGIN_FALLBACK 开关）+ compose（BFF_ADMIN_CRED_FILE=/data/admin_cred.json）。
- 重建 tests/test_admin_cred_selfheal.py（15 个，request mock 用 client_ip 签名适配新版）。
- 踩坑：hewapi 有 emoji 门禁（test_no_emoji 扫 app/*.py），补丁注释里的 ⚠️（U+26A0）撞门禁 → 已全部换文字，码点级验证清零。**以后给 hewapi 写注释禁用一切 emoji/符号字符**。另一失败 test_checker_detects_emoji_in_frontend 为基线遗留（static/docs.js）。
- compose config 校验：新克隆无 .env，插值变量（APP_VERSION/VCS_REF/BFF_SECRET_KEY/NEWAPI_*/ADMIN 账密）全 required —— 服务器上有 .env 不受影响；本地校验需临时 env 假值。终态 3 文件 +161/-14 + 新测试。

## hewapi 服务器部署口径（2026-09-20 11:25 确认）
- 服务器用的是「直拉镜像版」compose（image: ghcr.io/jushenzhidao/newapi-bff:sha-xxx，非仓库 build 版）→ 仓库 compose 的改动（BFF_ADMIN_CRED_FILE）**不会自动带过去**。
- 升级流程：飞哥 commit+push → Actions 构建 sha-<新tag> → 服务器改 image tag → docker compose pull bff && docker compose up -d bff。
- 服务器 compose 建议补一行 env：BFF_ADMIN_CRED_FILE=/data/admin_cred.json（不加也能跑：env PAT 直供+读回自愈都在内存完成，只是凭据缓存不落盘、重启后多一次读回登录）。

## flovart 服务器直拉镜像版部署文件（2026-09-20 11:29）
- 飞哥拍板：服务器部署改为「镜像 tag 写在 compose 里」（.env 换 tag 太复杂）。照 hewapi 直拉版风格产出两文件（语法已校验）：
  - `deploy/compose.server.yml`：无 build 段，image 行 ghcr.io/jushenzhidao/flovart-bff:sha-<tag>（升级只改这行），含 BFF_ADMIN_CRED_FILE=/data/admin_cred.json，端口回环 FLOVART_BFF_PORT:-8300，tmpfs 64m/memory 1G，卷 flovart-bff-data 绝对名。
  - `deploy/env.server.example`：.env 模板，**不含任何镜像配置**；迁移时删掉服务器旧 .env 的 FLOVART_BFF_IMAGE/APP_VERSION/VCS_REF。
- GHCR 镜像名按 build-image.yml（ghcr.io/<owner>/<repo> 小写）写的 jushenzhidao/flovart-bff，实际以 Actions 产物为准。
- 部署步骤：上传两文件 → .env 填值 → docker compose -f compose.server.yml pull bff → up -d bff。

## hewapi CI 修复（2026-09-20 11:40）
- dev push 后 CI「Lint & Test」exit 1 → 本地装 ruff==0.6.9（CI 锁版本，本地 0.16.7 误报不可信）精确复现：4 个违规全是我的补丁引入——app/newapi_client.py 两处 S110（except-pass 吞异常，改 logger.debug）、测试文件 I001（import 排序，from app import config 与 newapi_client as nc 拆行）+ S107（参数名 self_token 撞密码检测，改名 self_auth）。
- emoji 门禁实际通过（⭐ U+2B50 不在检测范围，此前担心的多余）。
- flovart CI（build-image.yml）刻意不跑 ruff（有注释说明 68+61 项历史债），只 pytest → flovart 无需同款修复。
- 教训：给 hewapi 写代码本地必须用 CI 同款 ruff 0.6.x 验证，独立 venv `~/.workbuddy/binaries/python/envs/ruff06`。最终三关（emoji/pytest 146/ruff）全过，可重新 push。

## 测试环境 .env 迁移（2026-09-20 11:47）
- 飞哥贴出测试环境旧 .env（2026-09-16 版，build 模式口径），要求按新直拉格式重写 → 产出 `deploy/env.server.test`（上传后重命名 .env）：现网值全保留（BFF_SECRET_KEY/管理员四件套 uid=1/WAVESPEED key/LOGFIRE v2 token/8310 端口/172.17.0.1），删除镜像三件套（FLOVART_BFF_IMAGE/APP_VERSION/VCS_REF）与构建期变量（PIP_INDEX_URL/PYTHON_IMAGE）。
- compose.server.yml 占位 tag 已换成现网实际版本 sha-5987645（新自愈镜像出了再改）。
- 配对校验：docker compose --env-file config -q 通过，image/端口插值正确。
- 待部署：hewapi CI 修好后 push 出新镜像 → 两台各换 tag + 换 .env。

## 2026-09-20 测试环境登录故障根因突破：「需要安全验证」
- 飞哥在 flovart 测试环境注册新用户报 400 `{"success":false,"message":"需要安全验证"}`——文案不在 BFF 代码里，是网关 new-api 原样透传 → 网关开了人机验证/Turnstile 类「安全验证」，服务端 BFF 登录无法提交验证码 → 全部 BFF 侧登录/注册被打挂。
- **串联解释此前所有假象**：①普通用户登录 401「用户名或密码错误」——auth.py 旧逻辑把上游任何 400 都改写成这句（真凶吞错）；②重置密码无效；③另一对接应用（hewapi 类）普通用户也登不进（同样走服务端登录撞验证码墙）；④网关日志里浏览器登录（官方前端带验证码组件）全 200、BFF 侧全挂。
- **代码修复**（D:/code/flovart-bff/app/routers/auth.py login）：删掉 `or e.status_code == 400` 的无差别改写，只有文案含 password/用户名或密码 才改写成 401，其余原样透传状态码+文案。142 passed。
- **待飞哥**：网关面板查「安全验证/Turnstile/人机验证」开关（设置→运营/登录注册），关掉或适配；这大概率就是 9/18 前后误开的。

## 2026-09-20 根因实锤 + rc.37 proof 适配完成
- **根因（源码+真机双实锤）**：网关升级 new-api v1.0.0-rc.37 后，`GET /api/user/token`（换 PAT，BFF 登录链路必经）被 `middleware/secure_verification.go` **无条件**要求 `X-Security-Proof` 头（scope=access_token.generate，无 2FA/Passkey 账号唯一方式=password）。缺失 → 403「需要安全验证」→ `_parse_response` 把 success:false 一律包成 **400**（newapi_client.py:451）→ 登录处又把 400 改写成 401「用户名或密码错误」。两天所有假象（改密码没用、两应用全灭、网关浏览器能登）全部闭合。Turnstile 开关无关。
- **真机端到端验证通过**（adam 测试号）：login(200, JWT 15min) → POST /api/verify {method:password, scope:access_token.generate} → 200 proof_token → GET /api/user/token + X-Security-Proof → 200 换到 PAT → DELETE session 归还。
- **代码修复**（flovart-bff）：新增 `_mint_access_token()`（password proof + 旧网关 404/502 优雅跳过），`login()` 与 `_admin_login()` 均接入；auth.py 吞错改写已删。新增 tests/test_login_secure_proof.py 5 用例，全量 **147 passed**。
- ⚠️ **读回自愈在 rc.37 失效**：`GET /api/user/self` 的 buildSelfUserData 不再返回 access_token → 跨机免轮换恢复没了。管理员 PAT 尽量保持 .env 长期有效；面板侧重新生成令牌现在也被 proof 拦（浏览器可交互完成），误作废风险大降。401 自愈兜底=login+proof+轮换（会作废对端，两机共用管理号仍需尽量避免同时失效）。
- **待办**：①newapi-bff(hewapi) 同款移植（登录链路一样会挂）；②两仓 commit+push→CI→两台服务器换 tag；③adam 测试号有 2 个探测遗留会话（30 天过期，可在面板登录设备管理清）。

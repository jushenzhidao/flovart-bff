
## Chatfire 异步生图接口调试（2026-09-20）
- 文档：POST /async/v1/images/generations（202→task_id/status=QUEUED）+ GET /async/v1/images/generations/{task_id}。基座 api.chatfire.cn。
- 经 workbuddy 网关（newapi-bff）实测，令牌 sk-KfTV...ht7D + New-Api-User:6：
  - **POST /async/* 是盲透传**：假模型也 202（创建成功≠模型可用），不做模型/渠道校验
  - **GET 查询走 distributor 按 model 找渠道**：模型不在用户分组的启用渠道里 → 503 model_not_found
  - gpt-image-2 全链路通：202 QUEUED → 202 IN_PROGRESS → 200 出图
  - **结果形态按模型不同**：doubao-seedream → data[].url（火山 TOS 签名 URL，86400s 过期）；gpt-image → data[].b64_json + revised_prompt
  - 任务态字段：task_id/status（QUEUED/IN_PROGRESS/终态待确认）/created_at/scheduled_at/batch_key/batch_state/replayed
- 遗留：doubao-seedream-5-0-pro-260628 不在 group default 任何渠道 → 飞哥需在面板把该模型加进 chatfire 渠道（image-2）后查询才通
- 接入方案待飞哥拍板：flovart-bff tasks.py 加 async 提交+轮询模式 vs model-plaza 侧接入

## Chatfire 异步生图接入 flovart（2026-09-20，已完成代码+测试）
- 飞哥拍板接入 flovart 画布生图。
- **config.py**：新增 `GATEWAY_ASYNC_IMAGE_SUBMIT_PATH`（默认 async/v1/images/generations）+ `GATEWAY_ASYNC_IMAGE_MODELS`（前缀匹配，默认 doubao-seedream）。
- **tasks.py**：`_is_chatfire_async_image_model`（前缀判定）+ `_submit_chatfire_async_image`（POST 裸 {model,prompt,image[]}，provider 记 **gateway-async-image**，size 等扩展参数不外发）+ `_poll_chatfire_async_image`（**data 键出现即终态**——完成响应无 status 字段；error 体/FAILED 系→failed 统一包 {"error":...}；空 data→无产物失败不记 succeeded）+ `_CHATFIRE_ASYNC_FAILED` 常量。
- submit() async 分支前置 chatfire 判定（kind=image-gen + 前缀命中 + mode=async）；get_task() 按 provider=gateway-async-image 分流。
- 踩坑：Edit 换函数时把 `_is_gemini_image_model` 的 def 行吞了（docstring 悬空），靠 pytest import 报错前自查 Read 发现修复。
- 测试：tests/test_chatfire_async_image.py 18 个，全套 **127 passed**。
- 前端零改动：mode=async 由 AI 服务配置 params.mode 带来（既有机制）。
- 待办：①飞哥面板把 doubao-seedream-5-0-pro-260628 挂到 chatfire 渠道（default 分组）→ 查询才通；②前端建服务配 mode=async；③BFF 出新镜像部署（连同 gemini 路由一起）；④两个已创建的 doubao 任务 task_id（doubao_seedream_00de27b789.../b22e68c5ff...）留着渠道修好后可复测查询。

## 管理员 PAT 互踢根治（2026-09-20，flovart-bff + hewapi 双侧落地）
- 痛点：两 BFF 共用 uid=1 + 同 PAT 连同一台 new-api。旧逻辑 401 → 盲目 `_admin_login()`（GET /api/user/token 轮换 PAT）→ 互作废对方 → 乒乓 → 50 会话/100 签发双爆 → 「登录设备数已达上限」死锁（new-api 会话永不过期）。
- 根治三件套（两仓库同构实现）：
  1. `_reload_admin_cred()`：401 先重读共享凭据文件，对端已轮换落盘就直接采纳，绝不 login；
  2. `_self_heal_admin_cred()`：文件无新值 → 锁内双检（①缓存 PAT 已变=同进程治愈 ②重读文件=跨进程治愈）→ 仍无效才 login，单飞锁防并发双轮换；`_save_admin_cred(force=True)` 强制落盘（env 直供也写）；
  3. `NEWAPI_ADMIN_LOGIN_FALLBACK=0` 可禁自动轮换转人工。
- 部署：两边 compose 共挂外部卷 `flovart-admin-cred` → /shared，`BFF_ADMIN_CRED_FILE=/shared/admin_cred.json`（flovart 负责 name+create，hewapi external:true 引用；先起 flovart 或手动 docker volume create）。flovart 的 BFF_ADMIN_CRED_FILE 从 /data/admin_cred.json 改到 /shared —— **旧缓存 PAT 作废需重新登录一次落盘**。
- 测试：flovart tests/test_admin_cred_selfheal.py 11 个（全量 138 passed）；hewapi 同款 11 个（全量 5 个失败为基线遗留：static/docs.js ✓ emoji 门禁 + docs 档案目录，与本次无关）。

## imageGenMode 改异步不生效（2026-09-20 傍晚，已修）
- 现象：管理员把平台服务改 async 后生图，BFF 日志仍 `params.mode=sync resolved_mode=sync`。
- 根因：flovart-web `utils/platformSharedEntries.ts` 的 `sameSharedEntries()` 只比 platformServiceId/name/isDefault/customModels，**漏比 imageGenMode/videoGenMode/imageGenModel/videoGenModel** → 服务配置变更被判「无变化」跳过重铺，用户本地影子条目停留旧 sync，只有重新登录（首铺无去重）才拿到新值。
- 修复：四字段补进比较（utils/platformSharedEntries.ts，未提交）。
- 链路备忘：影子条目 imageGenMode 缺省回填 'async'（platformSharedEntries.ts:69）；平台 Key 池条目（flovart_platform）写死 async（App.tsx:333）；params.mode=key.imageGenMode（aiGateway.ts:2861/4451/4550）。
- ⚠️ flovart-web 仓库 `.git/refs/` 整个丢失（git-repo-rescue 同款：HEAD/config/objects 在、refs 缺失）。我只重建了空 refs 目录（heads/tags/remotes/origin），**未写任何 ref、未 stash/pop**，随后按飞哥指令停止一切 git 操作。恢复 SHA 线索：main 本地/远端 tip=`769adfb5ca854ef1a64c60f1b2a9d69c17fc0fca`，origin/dev=`f87f30a4db44b2b86cb7582e45b3629f8e313aed`（reflog 在 .git/logs）。
- 飞哥新需求（待确认方案）：前端/BFF 请求路径要跟上游真实调用路径一致（devtools 能看出 sync/async），候选：镜像路由 /api/v1/images/generations 与 /api/async/v1/images/generations（对应 GATEWAY_ASYNC_IMAGE_SUBMIT_PATH=async/v1/images/generations）。

## 异步闭环 + 前端出包（2026-09-20 晚）
- 18:20/18:21 管理员+用户两笔生图均 `resolved_mode=async`（POST async/v1/images/generations 202 → ~2s GET 轮询 → 200 出图，57s/32s）——**sameSharedEntries 修复经真机验证闭环**，模式跟服务配置走、与账号无关。
- 方案 B（镜像路由）飞哥拍板，排 23:00 一次性自动化执行（预置决策 D1-D7：路径即语义优先于 params.mode、响应协议不变、只做图片、共用 tasks 流程、前端 USE_MIRROR_IMAGE_ROUTES 开关、零 git、pytest mock 验证）。
- 前端出包（含标注涂鸦全量迭代 + imageGenMode 修复）：`D:\code\flovart-web-dist-20260920-dirty.zip`（65 条目 / 4.6MB / CRC 全过 / 无顶层 dist/ 目录）。核对锚点：**assets/index-bGhE9VMH.js**；标注涂鸦改动在 WorkflowWorkspace-CNtOuKP9.js（grep「标注涂鸦图片」命中）。localhost 硬编码与旧包逐字一致=既有行为；0 sourcemap；构建 37.67s，旧 dist 已挪 /d/code/_flovart-web_prev_20260920-182817。命名无 git hash（不能跑 git），-dirty 后缀表工作区未提交。
- 应急脚本同步到 flovart：scripts/clear_sessions.py（auth_version 抬升吊销全部会话解死锁，⚠️ 连带作废该账号 PAT，用后必须重登换新）+ fix_session_limit.py。
- ⚠️ 面板「生成令牌」仍会作废 PAT：现在两边会经共享文件自愈（第一个 401 触发轮换、第二个 401 读文件），最多一轮收敛，不再乒乓。
- 飞哥本地 newapi-bff 不在 git 仓库内（.git 缺失），改动无法 commit——提醒过。

## ⚠️ 修正：两 BFF 不在同一台服务器（飞哥 2026-09-20 指出）→ 方案改为「读回恢复」
- 共享卷方案作废（Docker volume 跨不了机器，hewapi 侧 external 引用还会起不来）。两份 compose 已回退：各自用本地 /data/admin_cred.json 缓存，删除 flovart-admin-cred 卷。
- **新核心：读回恢复 `_recover_admin_pat_by_readback()`**：401 自愈序列 = ①重读本地凭据文件（同机多实例用）→ ②锁内双检 → ③**login 拿会话后 GET /api/user/self 把账号当前 access_token 原样读回来**（读操作不轮换不作废，对端服务器零感知）→ ④读回为空（账号没设过 token）才最后走轮换兜底。开关 `NEWAPI_ADMIN_PAT_READBACK`（默认 1）。
- 登录逻辑重构：抽出 `_admin_session_login()`（只建会话不轮换，用完必归还）；`_admin_login()` 仅剩轮换兜底职责。读回的 PAT 同样强制落盘。
- 测试：两边 test_admin_cred_selfheal.py 各 15 个（+4 读回用例：成功不轮换/空回落轮换/异常回落/会话归还）。flovart 全量 **142 passed**；hewapi 135 passed + 5 个基线遗留失败（docs.js emoji 门禁，与本次无关）。
- 部署注意：两台服务器各自出镜像重启即可，无共享卷/无部署顺序要求。旧 admin_cred.json 缓存里的 PAT 若已失效，首个 401 会触发读回自愈。
- 风险备注：`/api/user/self` 是否返回 access_token 字段未经线上实证（验证需登录，铁律禁自动试）。若某版本把它抹掉 → 读回恒空 → 自动降级为轮换兜底（安全但退化回互踢风险），部署后看日志有无 "recovered by readback" 即知。

## compose 终态确认（2026-09-20 10:47）
- 飞哥问「除镜像外 compose 要不要改」→ 复查发现 **flovart compose 残留** `BFF_ADMIN_CRED_FILE: /shared/admin_cred.json`（上一轮回退时 volumes 挂载和卷定义删了、但这行 env 没改成——/shared 不再挂载，read_only 容器里写凭据必失败）。已修为 `/data/admin_cred.json`。
- 终态：两边 compose 均为本地 /data/admin_cred.json、无共享卷、无 /shared，语法校验过。**服务器上 compose 文件需要同步更新（用本地这份），不是不用改**——准确说法：不用手工编辑，直接上传替换本地最新版即可。
- 教训：多段回退后要 grep 全量验证残留，别只看最后一段编辑成功。

## hewapi 重新拉取代码后重打补丁（2026-09-20 11:02）
- 飞哥 git 文件丢失、重新 clone（HEAD=36a2835，工作区干净）→ 我们的全部改动消失。
- 重新应用全套（新版代码结构与旧版一致，补丁一次命中）：newapi_client.py（+asyncio import、_save_admin_cred force、_reload_admin_cred、单飞锁、_self_heal_admin_cred、_admin_session_login、_recover_admin_pat_by_readback、_admin_login 重构、admin_request 401→自愈）+ config.py（READBACK/LOGIN_FALLBACK 开关）+ compose（BFF_ADMIN_CRED_FILE=/data/admin_cred.json）。
- 重建 tests/test_admin_cred_selfheal.py（15 个，request mock 用 client_ip 签名适配新版）。
- 踩坑：hewapi 有 emoji 门禁（test_no_emoji 扫 app/*.py），补丁注释里的 ⚠️（U+26A0）撞门禁 → 已全部换文字，码点级验证清零。**以后给 hewapi 写注释禁用一切 emoji/符号字符**。另一失败 test_checker_detects_emoji_in_frontend 为基线遗留（static/docs.js）。
- compose config 校验：新克隆无 .env，插值变量（APP_VERSION/VCS_REF/BFF_SECRET_KEY/NEWAPI_*/ADMIN 账密）全 required —— 服务器上有 .env 不受影响；本地校验需临时 env 假值。终态 3 文件 +161/-14 + 新测试。

## hewapi 重新拉取代码后重打补丁（2026-09-20 11:02）
- 飞哥 git 文件丢失、重新 clone（HEAD=36a2835，工作区干净）→ 我们的全部改动消失。
- 重新应用全套（新版代码结构与旧版一致，补丁一次命中）：newapi_client.py（+asyncio import、_save_admin_cred force、_reload_admin_cred、单飞锁、_self_heal_admin_cred、_admin_session_login、_recover_admin_pat_by_readback、_admin_login 重构、admin_request 401→自愈）+ config.py（READBACK/LOGIN_FALLBACK 开关）+ compose（BFF_ADMIN_CRED_FILE=/data/admin_cred.json）。
- 重建 tests/test_admin_cred_selfheal.py（15 个，request mock 用 client_ip 签名适配新版）。
- 踩坑：hewapi 有 emoji 门禁（test_no_emoji 扫 app/*.py），补丁注释里的 ⚠️（U+26A0）撞门禁 → 已全部换文字，码点级验证清零。**以后给 hewapi 写注释禁用一切 emoji/符号字符**。另一失败 test_checker_detects_emoji_in_frontend 为基线遗留（static/docs.js）。
- compose config 校验：新克隆无 .env，插值变量（APP_VERSION/VCS_REF/BFF_SECRET_KEY/NEWAPI_*/ADMIN 账密）全 required —— 服务器上有 .env 不受影响；本地校验需临时 env 假值。终态 3 文件 +161/-14 + 新测试。

## hewapi 服务器部署口径（2026-09-20 11:25 确认）
- 服务器用的是「直拉镜像版」compose（image: ghcr.io/jushenzhidao/newapi-bff:sha-xxx，非仓库 build 版）→ 仓库 compose 的改动（BFF_ADMIN_CRED_FILE）**不会自动带过去**。
- 升级流程：飞哥 commit+push → Actions 构建 sha-<新tag> → 服务器改 image tag → docker compose pull bff && docker compose up -d bff。
- 服务器 compose 建议补一行 env：BFF_ADMIN_CRED_FILE=/data/admin_cred.json（不加也能跑：env PAT 直供+读回自愈都在内存完成，只是凭据缓存不落盘、重启后多一次读回登录）。

## flovart 服务器直拉镜像版部署文件（2026-09-20 11:29）
- 飞哥拍板：服务器部署改为「镜像 tag 写在 compose 里」（.env 换 tag 太复杂）。照 hewapi 直拉版风格产出两文件（语法已校验）：
  - `deploy/compose.server.yml`：无 build 段，image 行 ghcr.io/jushenzhidao/flovart-bff:sha-<tag>（升级只改这行），含 BFF_ADMIN_CRED_FILE=/data/admin_cred.json，端口回环 FLOVART_BFF_PORT:-8300，tmpfs 64m/memory 1G，卷 flovart-bff-data 绝对名。
  - `deploy/env.server.example`：.env 模板，**不含任何镜像配置**；迁移时删掉服务器旧 .env 的 FLOVART_BFF_IMAGE/APP_VERSION/VCS_REF。
- GHCR 镜像名按 build-image.yml（ghcr.io/<owner>/<repo> 小写）写的 jushenzhidao/flovart-bff，实际以 Actions 产物为准。
- 部署步骤：上传两文件 → .env 填值 → docker compose -f compose.server.yml pull bff → up -d bff。

## hewapi CI 修复（2026-09-20 11:40）
- dev push 后 CI「Lint & Test」exit 1 → 本地装 ruff==0.6.9（CI 锁版本，本地 0.16.7 误报不可信）精确复现：4 个违规全是我的补丁引入——app/newapi_client.py 两处 S110（except-pass 吞异常，改 logger.debug）、测试文件 I001（import 排序，from app import config 与 newapi_client as nc 拆行）+ S107（参数名 self_token 撞密码检测，改名 self_auth）。
- emoji 门禁实际通过（⭐ U+2B50 不在检测范围，此前担心的多余）。
- flovart CI（build-image.yml）刻意不跑 ruff（有注释说明 68+61 项历史债），只 pytest → flovart 无需同款修复。
- 教训：给 hewapi 写代码本地必须用 CI 同款 ruff 0.6.x 验证，独立 venv `~/.workbuddy/binaries/python/envs/ruff06`。最终三关（emoji/pytest 146/ruff）全过，可重新 push。

## 测试环境 .env 迁移（2026-09-20 11:47）
- 飞哥贴出测试环境旧 .env（2026-09-16 版，build 模式口径），要求按新直拉格式重写 → 产出 `deploy/env.server.test`（上传后重命名 .env）：现网值全保留（BFF_SECRET_KEY/管理员四件套 uid=1/WAVESPEED key/LOGFIRE v2 token/8310 端口/172.17.0.1），删除镜像三件套（FLOVART_BFF_IMAGE/APP_VERSION/VCS_REF）与构建期变量（PIP_INDEX_URL/PYTHON_IMAGE）。
- compose.server.yml 占位 tag 已换成现网实际版本 sha-5987645（新自愈镜像出了再改）。
- 配对校验：docker compose --env-file config -q 通过，image/端口插值正确。
- 待部署：hewapi CI 修好后 push 出新镜像 → 两台各换 tag + 换 .env。

## 2026-09-20 测试环境登录故障根因突破：「需要安全验证」
- 飞哥在 flovart 测试环境注册新用户报 400 `{"success":false,"message":"需要安全验证"}`——文案不在 BFF 代码里，是网关 new-api 原样透传 → 网关开了人机验证/Turnstile 类「安全验证」，服务端 BFF 登录无法提交验证码 → 全部 BFF 侧登录/注册被打挂。
- **串联解释此前所有假象**：①普通用户登录 401「用户名或密码错误」——auth.py 旧逻辑把上游任何 400 都改写成这句（真凶吞错）；②重置密码无效；③另一对接应用（hewapi 类）普通用户也登不进（同样走服务端登录撞验证码墙）；④网关日志里浏览器登录（官方前端带验证码组件）全 200、BFF 侧全挂。
- **代码修复**（D:/code/flovart-bff/app/routers/auth.py login）：删掉 `or e.status_code == 400` 的无差别改写，只有文案含 password/用户名或密码 才改写成 401，其余原样透传状态码+文案。142 passed。
- **待飞哥**：网关面板查「安全验证/Turnstile/人机验证」开关（设置→运营/登录注册），关掉或适配；这大概率就是 9/18 前后误开的。

## 2026-09-20 根因实锤 + rc.37 proof 适配完成
- **根因（源码+真机双实锤）**：网关升级 new-api v1.0.0-rc.37 后，`GET /api/user/token`（换 PAT，BFF 登录链路必经）被 `middleware/secure_verification.go` **无条件**要求 `X-Security-Proof` 头（scope=access_token.generate，无 2FA/Passkey 账号唯一方式=password）。缺失 → 403「需要安全验证」→ `_parse_response` 把 success:false 一律包成 **400**（newapi_client.py:451）→ 登录处又把 400 改写成 401「用户名或密码错误」。两天所有假象（改密码没用、两应用全灭、网关浏览器能登）全部闭合。Turnstile 开关无关。
- **真机端到端验证通过**（adam 测试号）：login(200, JWT 15min) → POST /api/verify {method:password, scope:access_token.generate} → 200 proof_token → GET /api/user/token + X-Security-Proof → 200 换到 PAT → DELETE session 归还。
- **代码修复**（flovart-bff）：新增 `_mint_access_token()`（password proof + 旧网关 404/502 优雅跳过），`login()` 与 `_admin_login()` 均接入；auth.py 吞错改写已删。新增 tests/test_login_secure_proof.py 5 用例，全量 **147 passed**。
- ⚠️ **读回自愈在 rc.37 失效**：`GET /api/user/self` 的 buildSelfUserData 不再返回 access_token → 跨机免轮换恢复没了。管理员 PAT 尽量保持 .env 长期有效；面板侧重新生成令牌现在也被 proof 拦（浏览器可交互完成），误作废风险大降。401 自愈兜底=login+proof+轮换（会作废对端，两机共用管理号仍需尽量避免同时失效）。
- **待办**：①newapi-bff(hewapi) 同款移植（登录链路一样会挂）；②两仓 commit+push→CI→两台服务器换 tag；③adam 测试号有 2 个探测遗留会话（30 天过期，可在面板登录设备管理清）。

## 2026-09-20 rc.37 proof 修复上线验证 + workbuddy积分 移植
- **飞哥部署新镜像后实测：普通用户登录恢复** ✅ —— rc.37 proof 适配在生产验证通过。
- **命名澄清**：`D:/code/newapi-bff/newapi-bff` = 「WorkBuddy 积分」应用（flovart-bff 母项目），非网关。已写入 MEMORY.md。
- 同款修复已移植到 workbuddy积分 仓（_mint_access_token + login/_admin_login 接入 + tests/test_login_secure_proof.py 5 用例）：**151 passed**，ruff 0.6.9 全绿，无 emoji。
- 待飞哥：workbuddy积分 仓 commit+push→CI→换 tag 部署；网关 compose 抬 USER_SESSION_ACTIVE_LIMIT/ISSUANCE_LIMIT；管理号会话 DB 清理后重启 new-api 容器。

## 2026-09-20 收官：全部部署闭环
- 管理员 409 会话打满：phpMyAdmin 定位 newapi-bff 库 user_sessions，管理员=id1(newapi-bff, role=100)，UPDATE status='revoked' 清零 + 重启 newapi-for-bff，恢复登录。
- 网关 compose 已加 USER_SESSION_ACTIVE_LIMIT=200 / ISSUANCE_LIMIT=10000 / WINDOW_SECONDS=86400 并部署（注意：environment 是 list 风格，注释里的 mapping 写法不能直接抄）。
- workbuddy积分 已换新镜像 tag 部署 → 乒乓源头消失，rc.37 proof 适配双仓全部上线，互踢闭环。
- 网关 DB 排查经验：MySQL=宝塔宿主机 3396（不在 docker），容器经 host-gateway IP 连；宿主机 127.0.0.1:3396 连接拒绝是因为未绑 loopback，要用容器解析出的网关 IP 连。
- 遗留：两台 BFF 中期各配独立管理号（读回自愈在 rc.37 已死，共用管理号仍有互踢窗口）；adam 测试号 2 个探测遗留会话 30 天自动过期。

## 2026-09-20 标注参考节点位置/命名微调
- annotate-ref 分支：标注图节点位置改为原节点**右侧**（x+width+60，同行），节点 title 默认「标注涂鸦图片」（注意：节点显示名走 node.title，createWorkflowNode 的 spec.title 默认「图片」，metadata.name 不影响标题）。tsc 通过。
- 标注对话框补提示词输入：遮罩局部重绘原来提示词硬编码「根据标注修改图片」（用户发现无输入口）。AnnotateDialog 新增 maskPrompt TextArea（必填才能提交，模型下拉上方），annotate confirmation 带 prompt，InfiniteWorkflow 透传给 runWorkflowImageEditOperation。tsc 通过。
- 飞哥拍板：**砍掉遮罩局部重绘**，标注涂鸦只保留「新建参考图节点」单链路。删除 annotate(mask) confirmation 分支、对话框模型下拉/提示词输入/双按钮、InfiniteWorkflow 的 maskEditModels memo 与 supportsMaskImageEditing 导入；生成节点位置改为标注节点**右侧**（原节点 → 标注涂鸦图片 → 生成节点一行排开）；按钮文案改回「发送给 AI」。tsc 通过。
- 标注涂鸦「框选」崩溃修复：rect 预览在 new Image() 的 onload 异步回调里读 React 事件的 event.currentTarget（派发结束后为 null）→ TypeError。修复：进入 move 分支先把 canvas 捕获为局部变量 cv。经验：React 事件对象严禁在异步回调里引用 currentTarget。
- 标注涂鸦升级（飞哥需求，参考外部标注工具）：①架构重构为笔画历史栈（AnnotateStroke: brush/rect/text 三类，redraw 全量重绘），支持「后退/前进」按钮（Undo2/Redo2）与逐步撤销，清空仍保留；②新增「文字」工具：点画布落点 → 原位浮层输入框 → Enter/失焦提交，字号=粗细×4，颜色跟随取色器；③框选预览从快照回放改为全量重绘（顺带消灭 event.currentTarget 异步崩溃路径）；④img onLoad 加载后按历史重绘（防改尺寸丢笔画）。tsc 通过。
- 文字标注「点了没反应」修复：原位浮动输入框的 autoFocus 被 Modal 焦点管理/重渲染抢焦点 → onBlur 空值自动取消 → 输入框瞬间消失。重构为确定性交互：点画布只放位置锚点（虚线圈标记，纯 DOM 元素不碰焦点），文字在右侧面板 Input 输入，Enter 或「添加文字标注」按钮提交。经验：Modal 内画布上做原位输入编辑，别依赖 autoFocus+onBlur 链。

## 2026-09-20 异步队列服务机制实锤（通用包装器 + 路径白名单）
- 飞哥澄清：异步服务 = **通用异步队列包装器**——任意同步路径前加 `/async` 提交、GET 轮询、**终态返回原同步格式**。但实测有**路径白名单**（不在名单 → 403 `path not in allowlist`/`permission_error`）。
- **gpt-image 系异步实测通过**（sk-tQLo... 网关令牌，GET /v1/models 可见 6 个 gpt-image*）：POST async/v1/images/generations（与 doubao 同路径已在白名单）→ 202 {task_id, status:QUEUED} → 202 IN_PROGRESS 数轮 → **200 {created, data:[{revised_prompt, b64_json}]**（原 images 同步格式），~60s 终态，b64 2.7MB。实测模型为 gpt-image-1（列表第一个），同路径机制下 gpt-image-2 全系同理。
- 终态判定可加强：pending 时 HTTP 202 + status 字段（QUEUED/IN_PROGRESS），完成时 HTTP 200 + data[]——现 BFF 的 data[] 判定兼容，可再加 status/HTTP 判定提速。
- **gemini 异步被白名单拦**：async/v1beta/models/{model}:generateContent → 403 path not in allowlist。需在异步服务配置加白后才能通；BFF 届时需扩展（gemini 提交路径 + 原生格式终态判定 + i2i inlineData parts）。
- BFF 接 gpt-image 异步只需配置：GATEWAY_ASYNC_IMAGE_MODELS 加 `gpt-image` 前缀 + 前端该服务 mode=async（或管理台 image_model_modes），零代码。

## 持久化对齐即梦模式：媒体血缘列 + 历史记录一等公民（晚间，飞哥拍板"按建议做"）
- **① cloud_media 加血缘列**（Pg db.py SCHEMA+migrate() ALTER IF NOT EXISTS / Local cloudstore.py CREATE+PRAGMA 补列双套）：`source_request_id`（关联 cloud_request_log.request_id）、`source_kind`（upload/generated）、`width/height`、`thumb_key`、`deleted_at`（软删预留，行为未改）+ `idx_cloud_media_src` 索引。
  - ⚠️ SQLite 迁移坑：初始 executescript 里**不能**建 `idx_cloud_media_src`（旧库列不存在 → no such column），必须放 PRAGMA 补列**之后**。
  - media_index_put/media_put 加可选 source_request_id/source_kind；media_index_get 读回这两个字段。
- **② 写入链路**：tasks.py `_persist_outputs` 加 `request_id` 形参，5 个调用点（chatfire 轮询/_run_sync/_external_call_and_record/thirdparty/gateway async）全传 → 产物 media 行 `source_kind="generated"` + 血缘；cloud.py media_upload → `source_kind="upload"`。
- **③ 历史接口**：`GET /api/me/history/records?limit&offset&kind`（cloud.py，require_session）→ {items, total}，items 含完整 payload+result（strip_b64 剥离），支持一键同款重提交。cloudstore 新增 reqlog_history（Pg/Local 双套）+ request_log_history 包装。
- **测试**：新增 tests/test_media_lineage_history.py 5 用例（存量库迁移/血缘写入读回/分页 total kind 过滤 uid 隔离/b64 剥离/端点）；3 个旧测试文件的 fake_persist 桩补 `request_id=None` 形参。**全量 152 passed**。
- **前端历史页 UI 切换未做**（刻意）：historyCloudSync.ts 用 cloud_docs(scope=history)+缩略图上传，切数据源是 UI 级改造，留给下一轮；新旧路径可并行。本地 BFF 未重启，新代码待 23:00 自动化任务重启后生效。

## 服务器接入 PostgreSQL（飞哥"我不会做"，直接改好部署文件）
- **docker-compose.yml**：新增 `postgres` 服务（postgres:16-alpine，container_name=flovart-pg，**刻意不发布端口**仅 compose 内网可达）+ 绝对名卷 `flovart-pgdata`；bff 加 `depends_on: postgres: service_healthy`；environment 补 POSTGRES_HOST/PORT/USER/PASSWORD（PASSWORD 用 `:?` 必填校验，DSN 留空走分项拼装）。`docker compose config` 渲染验证通过。
- **.env.server / .env.example**：数据层段改为「只需填 POSTGRES_PASSWORD（openssl rand -hex 16），其余默认」。
- 飞哥服务器侧步骤：①上传/同步新 docker-compose.yml ②.env 填 POSTGRES_PASSWORD ③docker compose pull && up -d ④验证 /readyz + `docker logs flovart-bff | grep -i postgres`（应见"连接池就绪"）。
- ⚠️ 遗留：服务器卷里旧 SQLite 数据不会自动进 PG，需一次性搬迁脚本（表结构两套一致，逐表 INSERT）；已向飞哥提出，待他拍板再写。

## 部署文件对齐服务器格式：compose.server.yml 内置 postgres（晚间，飞哥贴出服务器实际文件后）
- **关键澄清**：服务器实际用的是 `deploy/compose.server.yml`（直拉镜像版）+ 新格式 `.env`，此前我改的根目录 docker-compose.yml（build 模式）不是服务器在用的那份。飞哥明确：**以后部署文件一律以服务器格式为基准**。
- **deploy/compose.server.yml**：新增 postgres 服务（postgres:16-alpine / flovart-pg / 卷 flovart-pgdata / 无 ports / pg_isready 健康检查）+ bff 加 depends_on service_healthy + 云数据库段补 POSTGRES_HOST/PORT/USER/PASSWORD（PASSWORD 用 :? 必填）；image tag 同步为服务器现值 sha-71cbf31。`docker compose -f config` 渲染验证过。
- **deploy/env.server.example / env.server.test**：云数据库段统一为「只填 POSTGRES_PASSWORD，其余默认」格式。
- **.env.server 整体重写为服务器新格式**（旧 9/16 格式作废：删 APP_VERSION/VCS_REF/FLOVART_BFF_IMAGE/PIP_INDEX_URL/PYTHON_IMAGE 段），真实值与服务器现网逐值对齐（含 NEWAPI_ADMIN_PAT=a1q7...、POSTGRES_PASSWORD=22b3...）。
- 根目录 docker-compose.yml 与 .env.example 的 postgres 段此前已改好，本次核对一致，无需再动。
- 服务器侧步骤：同步 compose.server.yml → .env 确认 POSTGRES_PASSWORD → docker compose pull && up -d → `docker compose ps` 双 healthy + 日志 grep「连接池就绪」。
- ⚠️ 遗留：服务器 flovart-bff-data 卷里的存量 SQLite 数据不会自动进 PG，一次性搬迁脚本待飞哥拍板。

## 🔴 真 bug：lifespan 从未调用 db.init_pool()（服务器接 PG 当场暴露）
- **症状**：服务器 compose+postgres 都起来了、容器 healthy，但 `docker logs flovart-bff | grep -i postgres` 全空——因为 init_pool 根本没被调用，连「回落 SQLite」的日志都不会有。
- **根因**：docs/storage-architecture.md:78 明确写了「lifespan 启动 db.init_pool() / 关闭 db.close_pool()」，但 main.py 从没实现。以前生产一直 USE_PG=False（LocalMeta 不需要池）所以从未暴露；第一次配 PG → PgMeta._c() 返回 db.pool()=None → 云端文档/媒体/请求日志接口全会 500，表也不会建。
- **修复**：main.py `_lifespan` 启动段加 `await db.init_pool()`（置于首个数据访问 request_log_fail_stale 之前）、关闭段加 `await db.close_pool()`。全量 152 passed；本地 8301 冒烟见 `bff.db: POSTGRES 未配置，元数据回落本地 SQLite 后端` 日志正常打印。
- **部署链**：代码修复 → 飞哥 commit/push → CI 出新 sha tag → 改 compose.server.yml image 行 → `docker compose pull && up -d` → 验证日志「PostgreSQL 连接池就绪」+ `docker compose exec postgres psql -U flovart -d flovart -c '\dt'` 见 4 张表。
- ⚠️ 修复镜像上线前，服务器当前状态 = USE_PG=True 但无连接池：BFF 显示 healthy 但云端数据接口全 500（/readyz 只查配置不查池）。急用可临时回滚旧 compose（无 postgres 段那份）。

## ✅ 生产 PG 接通确认（20:21）
- 飞哥 push 后 CI 出 sha-c38dc51，服务器 compose pull+up：`bff.db: PostgreSQL 连接池就绪：postgresql://flovart:***@postgres:5432/flovart` 日志出现，init_pool 修复生产验证通过。flovart-pg healthy。剩余验证：\dt 见 4 表、页面画布/历史实际可用。
- cloud_media 13 列确认（含 source_request_id/source_kind/width/height/thumb_key/deleted_at），血缘迁移生产生效。服务器侧验收完毕，剩飞哥页面实操。

## SQLite → PG 一次性搬迁脚本（scripts/migrate_sqlite_to_pg.py，晚间）
- 设计：容器内运行（镜像自带 sqlite3+asyncpg、同 compose 网络可达 PG）；SQLite `PRAGMA query_only=ON` 零写入；PG 侧 `INSERT ... ON CONFLICT DO NOTHING`（**冲突保留 PG 现值**，保护切换后新数据，可安全重复执行）；列取 SQLite∩PG 交集（旧 8 列库无血缘列也能迁）；ISO 时间串转 datetime 再给 asyncpg；PG 空库自动建表（同 db.py SCHEMA+ALTER 口径）；--dry-run 只统计。
- 本地实测（临时 PG 容器 + 旧 schema fixture）：dry-run/迁移/幂等重跑/冲突保护/增量补迁 5 项全过；中文/NULL/空格分隔时间边界正确。测试后已清理容器与临时目录，全量 152 passed。
- 服务器执行：commit/push 后新镜像自带脚本 → compose 目录里先 `--dry-run` 再正式跑；或免重建镜像：`docker compose run --rm -v ./scripts/migrate_sqlite_to_pg.py:/m.py bff python /m.py`。
- 服务器实跑两个坑修复：①宝塔上传脚本默认 600 权限 → 容器非 root 读不了 → chmod 644；②脚本挂载成 /m.py 时 sys.path 只有 `/`，而 app 包在镜像 WORKDIR /app → ImportError。脚本已修死（补 sys.path.insert(0, os.getcwd())），存量老脚本可用 `-e PYTHONPATH=/app` 临时救急。
- 搬迁脚本服务器实跑第三个坑：老 SQLite schema 的 cloud_request_log.payload_json 无 NOT NULL → 存量行有 NULL → PG NOT NULL 拒收（NotNullViolationError）。脚本已加 NOT_NULL_FALLBACK（payload_json→'{}' 等全 NOT NULL 列兜底）+ REQUIRED_COLS 脏行跳过计数；本地临时 PG 复测（含 NULL 行 fixture）通过，幂等重跑零新增，152 passed。服务器实况：cloud_docs 已迁 92（1 行冲突保留 PG 现值）、cloud_media 已迁 1102，只差 request_log 续迁——重跑同命令即可（幂等）。
- 🔴 搬迁第四坑（真根因实锤）：服务器 sanitize 报 `no such column: payload_json` → 服务器老 SQLite 的 cloud_request_log **根本没有 payload_json 列**（极老 schema）。此前「NULL 数据」判断有误——是列缺失：脚本取 SQLite∩PG 列交集时把 payload_json 静默丢掉 → INSERT 不含该列 → PG NOT NULL 无默认 → 全表必败。本地此前 fixture 都有该列，故复现不了。
- **修复**：脚本缺列时若在 NOT_NULL_FALLBACK 中则补默认值（打印「源库缺列补默认 [...]」）；SELECT 只取源库实际拥有的列。本地用无 payload_json 列的 fixture 复测：迁移/幂等/数据核对全过。
- 服务器剩余动作：重新上传本地 scripts/migrate_sqlite_to_pg.py → chmod 644 → `docker compose run --rm -e PYTHONPATH=/app -v ./migrate_sqlite_to_pg.py:/m.py bff python /m.py`。sanitize 步骤不需要了。
- ✅ SQLite→PG 搬迁完成（21:05）：cloud_docs 93 / cloud_media 1103 / cloud_request_log 379（payload_json 缺列补 '{}'），shares 0。剩飞哥开网站实操验证画布/历史/素材。这场从接 PG 到搬完的马拉松收官：compose 内置 PG → init_pool bug → 脚本权限/路径/缺列四连坑全记录在案。

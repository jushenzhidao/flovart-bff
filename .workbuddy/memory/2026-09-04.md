# 2026-09-04 工作日志

## 图片异步任务框架
- 画清整体架构（3 张 SVG）：整体分层 / 任务状态机 / 前端生命周期。结论：所有图片同步任务统一转异步，复用视频「提交→轮询→取结果」范式；放大/抠图/分层调第三方不可控，反而更该优先转异步。
- 输出接口契约文档 `IMAGE-ASYNC-TASKS-CONTRACT.md`：BFF `POST/GET/DELETE /api/tasks` + `Task` schema + 各 type(params 对齐现有前端请求体) + 前端 `imageTask.ts` 改造 + 第三方 client 保护(Semaphore/超时/重试) + 待确认决策点(§10)。
- 探查确认：BFF 端点统一 `{success,message,data}` + `require_session` + `/api` 前缀；图片功能(image-gen/upscale/remove-bg/split-layers)现为前端同步直连网关，仅视频走 `contents/generations/tasks` 异步；前端已有 `runtimeGeneration.ts` 轮询范式(2s/10min)可复用。
- 状态：文档待飞哥过目，未落地代码。
- 15:17 飞哥确认决策点：①产物存云端 media ②视频保留直连不纳入 BFF tasks ③BYOK 保持浏览器直连、BFF tasks 仅服务 hosted（推荐，待最终确认）④本期必选 Redis（任务表+队列）⑤MAX_CONCURRENT=8/UPSTREAM_TIMEOUT=300s 合适。文档已更新 §1/§2/§6/§7/§9/§10。下一步按落地顺序动手（待飞哥说开始）。
- 15:23 飞哥澄清第3点：第三方 API 也走我们网关、用平台同一 key、用户不关心 key、计费 BFF 自适配。据此**取消 BYOK 直连分支**——所有图片任务（含放大/抠图/分层）统一 hosted 经网关、同一平台 key。文档 §1 边界表+核心边界、§7.3 标题改为「上游调用保护」(BFF 只调自有网关、不直连第三方)、§10 第3点改 ✅ 已定稿。五项决策全 ✅，待飞哥说「开始」即落代码。
- 15:40 落地图片异步任务框架（代码）：①BFF app/tasks.py(REDIS_URL 持久化+内存降级 store、asyncio.Semaphore(8) 限流 worker 池、dispatch 4 handler、上游保护=gw_client 长超时300s+重试退避)、app/routers/tasks.py(POST/GET/DELETE /api/tasks，require_session 鉴权+owner 校验)、main.py 挂载+lifespan start/stop、config 加 REDIS_URL/TASK_MAX_CONCURRENT(8)/TASK_UPSTREAM_TIMEOUT(300)/TASK_QUEUE_MAX(200)/TASK_UPSTREAM_RETRIES(2)、requirements 加 redis>=5、newapi_client 加 client 超时透传；②前端 services/imageTask.ts(submit/get/cancel/poll/fetchMediaAsDataUrl/fetchFirstMedia，复用 hostedClient 会话 Cookie)+aiGateway.generateImageWithProvider 的 chatfire 分支改走 BFF 异步。验证：BFF 端口 9123 启动成功(内存降级)、POST /api/tasks 未登录返 401(路由+鉴权 OK)；前端 `tsc --noEmit` 全量 0 错误。upscale/remove-bg/split 的 BFF handler 已实现，前端调用点(runImageAgentWithProvider/splitImageLayersWithProvider)仍直连，待接 BFF tasks（下一轮）。

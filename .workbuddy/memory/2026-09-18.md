
## 01:40 三个 gemini 生图失败后续：第二层根因已直接修复（new-api 渠道）
- 飞哥反馈「重新上架也没用」→ 确认是上轮说的第二层：new-api 网关渠道没有这 3 个 gemini 模型。
- new-api 管理端（newapi-bff.oneapis.cn）认证方式：/api/user/login 返回 data.access_token（data.user.id=uid），后续请求要带 `Authorization: Bearer <access_token>` + `New-Api-User: <uid>`；用 session cookie 打 /api/* 会 401。
- 渠道 30「image-2」（group=default, type=1, chatfire 上游）模型清单只有 gpt-image 系列 → 503 No available channel 的实锤。
- ⚠️ PUT /api/channel/ 全量更新会报 Invalid parameters（fork 隐藏 key 字段，GET 不回传，全量提交体校验不过）；**最小字段 PUT {id, models} 反而成功**（status 200 success），其他字段不受影响。
- 已把 gemini-3.1-flash-image-preview / gemini-3.1-flash-lite-image / gemini-3-pro-image-preview 追加进渠道 30。
- ⚠️ 登录限频：flovart.oneapis.cn 与 newapi-bff.oneapis.cn 的 /api/user/login 短时间多次登录触发 429（按 IP，窗口约几分钟）；`.env` 的 NEWAPI_ADMIN_PAT 不是 sk- 形态且已失效，打 /v1 会 Invalid token。验证网关中继用「管理 API 建临时令牌（POST /api/token/ → 列表项 key 字段）→ 打 /v1/images/generations → DELETE 令牌」的套路。

## 10:50 gemini 网关冒烟结论 + 用户/管理员链路差异定案
- 冒烟脚本（smoke_gemini_gateway.py）跑通但验证无效：GET /api/token/ 列表项的 key 字段是**掩码形态**（len 18，非完整 48 位），拿去打 /v1 必 401 Invalid token → 「临时令牌验证」此路不通，别再试。取完整 key 的正确姿势是 POST /api/token/ 响应体本身（未验证）或控制台复制。
- 链路差异定案（回答飞哥「为什么用户和管理员不一样」「gpt 为什么能用」）：
  - 管理员自有卡片 = 浏览器直连 chatfire（BYOK）；普通用户用发布服务 = BFF → new-api 网关 → chatfire 渠道 30（9-14 拍板的 dev 语义）。
  - gpt 能用 = 渠道 30 模型清单一开始就含 gpt-image 全家；gemini 失败 = 清单里没有 → 503。
  - 已修：3 个 gemini 已追加进渠道 30（09:05）。
- 复现矩阵（chatfire 直连，本地/测试环境同 key）：配置名(带 -preview) 全部 200 出图；GA 名 gemini-3-pro-image / gemini-3.1-flash-image → 503「无可用渠道」。前端源码（v5 包）解析链发的是正确配置名 → 前端无 bug，卡点在网关渠道。
- 待办：后台任务 aclBj0（等 10min 限频后走 BFF /api/tasks 用户链路验证 3 个 gemini）；通了即闭环。「设为默认」修复仍未打包（v6 待出）。

## 10:55 验证排队与限频教训
- 两个站点（flovart.oneapis.cn / newapi-bff.oneapis.cn）的 /api/user/login 登录限频窗口远超 10 分钟（整夜多次登录触发），且疑似共享限流窗口 → 429 后短间隔重试只会延长窗口。
- 「临时令牌」验证套路修正：POST /api/token/ 的响应 data.key 可能带完整 key（列表接口的一定是掩码，len≈18，勿用）；key 非 sk- 开头要补前缀。
- 已挂 30 分钟安静期后的最终验证（后台任务 FUB6Rb）：复查渠道30 → 建临时令牌（POST 响应取 key）→ 打 /v1 三个 gemini → 删令牌。
- 结论性事实不变：chatfire 上游配置名 100% 出图；渠道 30 已含 3 个 gemini；前端 v5 发的模型名正确。用户界面验证是最快路径。

## 管理员请求日志查询接口落地（飞哥拍板后实现）
- 新接口（require_admin）：
  - `GET /api/console/requests?username=&uid=&kind=&status=&model=&limit=&offset=` — 全站用户调用日志列表；username 经 `na.admin_resolve_uid_by_username` 精确解析成 uid（找不到透传 404）；kind/status 精确、model 子串；列表摘要行补了 uid 字段并回填 username（admin_list_users 翻页 ≤5×100 解析，失败静默降级为空串不 500）
  - `GET /api/console/requests/{request_id}` — 单条详情（完整 payload+result），result/payload 里超长 b64 字段替换为 `<base64:N chars>` 占位
- cloudstore：`request_log_admin_list(uid,kind,status,model,limit,offset)` Pg/Local 双实现（Pg ILIKE / SQLite LIKE），facade 同名；`_pg/_local_reqlog_summary` 补 uid
- 测试：tests/test_console_requests.py 7 例（鉴权行为级 401 检查×2、筛选透传、username 解析、映射降级、b64 剥离、404）；全套 80 passed；Local SQL 用临时目录实测 6 组筛选组合全对
- 教训：直接调路由函数返回的是 resp.ok() 壳，断言要取 ["data"]；Depends(require_admin) 静态探测不可靠（monkeypatch/dependant 结构坑），改用 TestClient 未登录 401 行为验证
- 未做（仍待拍板）：①chat 对话落库 ②409 MODEL_SUSPENDED 落库前被 raise 导致失败无痕 ③30 天保留清理。部署提醒：此改动纯服务端，要出 BFF 新镜像才能在测试环境生效

## 两个日志盲区修复（飞哥拍板，已完成）
- 盲区① 409 下架拦截无痕：routers/tasks.py `create_task` 的 `assert_model_available` 抛 ModelSuspendedError 前现在先落一行 status=failed + mode=blocked + result={code,reason,message}；BYOK(_gateway) 不受影响
- 盲区② chat 不落库：routers/chat.py 全链路落库
  - 提交即落 submitted（kind=chat, mode=stream, payload 过 strip_b64）
  - `_logged_stream`（模块级，可测）包 StreamingResponse：透传同时旁路 `_sse_extract` 增量解析 → 正常结束 succeeded（result.reply 截尾 8000 字 + finish_reason/usage）；error 事件（含 BFF 自造 _sse_error）→ failed；客户端断开 GeneratorExit → cancelled；其他异常 → failed
  - resolve_user_key 502 也回写 failed（不留僵尸 submitted 行）
  - 日志层任何异常只 warning，绝不影响聊天主流程/409 透传
- `strip_b64` 上移 cloudstore 共享（console/chat 共用），新增 data-uri（image_url.url 以 data: 开头超长）分支
- 测试：tests/test_log_blindspots.py 9 例；全套 89 passed。教训：Python bytes 字面量不能含中文，要 .encode("utf-8")
- 状态：本地已全部完成未部署；与上一条管理员查询接口一起出 BFF 新镜像才在测试环境生效

## 登录 429 根因确诊（13:00 前后）
- 现象：飞哥管理员登录测试站一直 429「操作过于频繁」，换网络无效
- 根因：new-api `USER_SESSION_ISSUANCE_LIMIT`（默认 100 次/86400s=24h，**按账号**计、计数在 DB）被昨天我的自动登录脚本打爆；BFF 服务端转发登录 → 上游看到的是服务器 IP，切本机网络无用
- 修复：new-api 的 .env 加 `USER_SESSION_ISSUANCE_LIMIT=10000` 重启（等飞哥操作）；期间可用 docker exec 读容器 SQLite cloud_request_log 兜底查日志
- 🔴 铁律已写入 ~/.workbuddy/MEMORY.md：禁止自动登录/重试脚本，限频敏感端点失败一次即停转人工

## 管理员鉴权根本解法（飞哥拍板方向：密钥常态、账密兜底）14:00
- 死循环定性：new-api PAT=账号单值字段，GenerateAccessToken 每次生成即轮换 → 人用账密登录（na.login 会换 PAT）→ BFF 配的 PAT 作废 → BFF 兜底重登 → 再轮换+烧会话签发额度；且 uid=1 与 hewapi 共用
- 方案（即 deployment.md 最高优先级待办落地）：① new-api 建 flovart-admin 机器账号（role≥10）② 服务器 .env 换新账号、NEWAPI_ADMIN_PAT 留空 → 冷启登录一次落盘 /data/admin_cred.json 后永远走 PAT ③ hewapi 不动 ④ USER_SESSION_ISSUANCE_LIMIT=10000 防呆 ⑤ 纪律：机器账号密码不进浏览器
- 待办：等限频解除/调大后由飞哥在面板建号 + 改服务器 .env 重启

## PAT 验证记录（14:00）
- 飞哥更新本地 .env NEWAPI_ADMIN_PAT（前缀 a1q7）→ 单次探测 GET /api/user/self → **401 invalid access token**
- 按铁律只探一次即停。嫌疑：①token 在错误实例（workbuddy）上生成 ②复制到掩码值 ③uid 不匹配 ④多次生成互相作废（仅最后一次有效）
- 代码确认：env PAT 优先级最高（newapi_client.py:110，"no session consumed"），配了 env 就永不走账密/落盘缓存

## 日志接口文档落盘（15:00）
- 新增 `docs/request-log-api.md`：管理员/用户日志查询接口契约（console/requests 列表+详情、me/requests、参数表、F12/curl/docker exec 兜底用法、落库时机对照、b64 剥离与 BYOK 边界说明），遵循 docs/ 目录既有契约文档惯例

## 图生图 502 排障 + detail 透传 + stale 清扫（14:00-14:20）
- 用户 uid=171 图生图（gpt-image-2.5-flare，mode=sync）一直失败；日志 result 只有通用文案「上游服务暂时不可用」status=502 stage=request
- 定性：`newapi_client.py` 的 `except httpx.HTTPError` 把网络层异常吞成通用 502 —— 请求**没到模型**，真因（连接重置/413/超时）全丢。耗时 33s（< 300s 超时）排除超时；头号嫌疑 = **请求体 70MB+（53.5MB PNG base64）被网关 nginx client_max_body_size 拒掉掐连接**
- 修复① NewApiError 加 `detail` 字段（httpx 异常类型+原文 / 非 JSON 响应预览 body_preview 300 字），`_error_record` 落进 result.error.detail，服务器日志同步打印；用户文案不变
- 修复② 启动清扫：`cloudstore.request_log_fail_stale(hours=6)`（Pg make_interval / Local Python 比较），main.py lifespan 启动时把超 6h 仍 submitted 的行标 failed（type=stale_submitted）——治 03:02/03:27/03:33 那类僵尸行
- 测试：tests/test_error_detail.py 3 例 + tests/test_stale_sweep.py 1 例；全套 93 passed
- 待飞哥：①让该用户换小图（<2MB）重试验证 body size 假设 ②查网关 nginx client_max_body_size（workbuddy 实例）调大 ③要不要前端提交前压缩参考图（7347×5642 过分了）

## 前端参考图上传压缩（flovert-web，14:15-14:40）
- 飞哥拍板：最长边 4096 / JPEG 0.9。base64 本身压不了（固定+33%），收益全在编码前压图
- 新增 utils/fileUtils.ts `compressDataUrlForUpload(dataUrl)`：
  - 最长边>4096 → 等比缩；无透明 → JPEG 0.9；**有透明（蒙版）→ 保 PNG**（128px 探针画布扫 alpha）
  - ≤2MB 且尺寸达标 / SVG / GIF → 原样返回；重编码反而更大 → 保原样；任何异常 fail-open 返回原图
  - ⚠️ jsdom 里 Image.onload 永不回调 → 曾致 9 个测试超时（18 失败）。修复：canvasSupported() 探针（getContext 2d 为 null 即跳过）+ 解码 8s 超时 race
- 接入点 aiGateway.ts 3 处：① materializeOpenAIImageReferences（覆盖 hosted 图生图/BYOK/editImage/chat 引用，即本次血案路径）② editImageWithProvider 的 toDataUrl（base 图+mask，改 async）③ remove-bg/upscale 工具路径
- 发现 fileUtils.validateAndResizeImage 是死代码（零调用）——参考图此前完全没压
- 测试：抽查 5 套件与基线逐一对齐；全量 36 failed vs 基线 37 failed（历史遗留），零新增
- 提醒：前端改动要出包才生效；nginx client_max_body_size 仍建议调大（alpha PNG 保 PNG 可能仍大）

## @ 弹框缩略图（flovert-web，14:42-14:50）
- 飞哥需求：@ 引用弹框「画布节点」分区显示真实缩略图（与输入框 mention chip 一致），此前只有图标
- 根因：MentionList 只做同步判断（isFetchableMediaHref(thumbnail)），而画布节点 thumbnail=metadata.href 常为空/失效 blob，真实字节按 storageKey 在 workflowMediaStorage；只有 MediaMentionExtension NodeView（chip）做了异步解析
- 修复：MentionList.tsx 新增 `MentionThumb` 组件——direct thumbnail → onError/空时按 storageKey loadWorkflowMediaBlob（含云端恢复）→ objectURL 展示 → 失败降级类型图标；objectURL 在 effect cleanup 里 revoke。主列表 + 文件夹悬浮面板两处替换
- 组装侧（toWorkflowMentionItems / WorkflowNodePromptBar referenceItems）本就带 storageKey，无需改
- 验证：tsc 对 MentionList 零错误；5 个失败用例与基线逐一对照全部历史遗留，零新增

## @ 弹框两轮修复：storageKey 透传 + 名字对齐画布（14:52-15:05）
- 第二轮反馈①：弹框缩略图仍不显示 → 根因 mergeCanvasMentionItems 把 canvasItems 的 storageKey 丢了（类型签名没收该字段，map 未透传）→ 未连线节点全走这条分支。修复：签名加 storageKey? + 透传
- 第二轮反馈②：「图片名没对应上」→ 画布「图片 4」在弹框显示「图片 1」。根因：toWorkflowMentionItems label 用按连线顺序临时重编的 ordinal，与画布持久标题错位，且和未连线节点真名撞车（两条「图片 1」）
- 修复：toWorkflowMentionItems 与 toImageReferenceChips 统一 label=节点 title（真名），ordinal 降级为 aliases（手动输入 @图片N 仍可匹配）；安全依据：提交绑定文案 buildPromptWithReferenceBindings 的 ordinal 是提交时独立重编 + nameNote 带节点真名，不依赖 chips label
- 测试：workflowReferenceIntegrity「chips 与 mentions label 一致」约束两函数同步改后全过；3 套件 55 passed，剩 2 个 workflowEditor 失败为基线历史遗留

## PAT 恢复有效（15:13）—— 改库固定值方案
- 互踢根因确认：uid=1（newapi-bff）被 flovart-bff 与 hewapi 共用，任何一方兜底重登（GET /api/user/token）/面板点生成都轮换 access_token → 对方 PAT 当场作废。飞哥面板生成的令牌就是这么被杀的
- 飞哥直接改 DB 把 access_token 固定为 .env 里的值（绕过 GenerateAccessToken 随机轮换）→ 单次探测 GET /api/user/self → **200，username=newapi-bff role=100 id=1**
- ⚠️ 改库不是免疫：hewapi 触发兜底重登仍会覆盖这个值 → 分家（flovart-admin 独立账号）仍是根治方案，待执行
- 待办：服务器 flovart-bff .env 同步这把 PAT + 重启容器，管理通道全走密钥

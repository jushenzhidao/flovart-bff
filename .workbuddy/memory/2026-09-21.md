## MinIO OSS 接入收尾（承接 2026-09-20 被取消的清理）
- 补齐上次 499 canceled 的清理：moto 进程（9001 端口）已杀、tmp_oss 临时目录已删、docker 无测试容器残留、pytest 152 passed 回归通过。
- 确认代码侧就绪：app/config.py 全套 OSS_* 变量、app/oss.py boto3 S3 兼容（endpoint+addressing_style 消除云差异）、compose.server.yml 与 env.server.example 已透传；OSS_PROVIDER 仅文档性不参与逻辑 → MinIO 无需改代码。
- 交付服务器配置步骤（.env 加 OSS_ENABLED=1 / OSS_ENDPOINT=https://cn.s3ai.cn / OSS_ADDRESSING_STYLE=path 等）；⚠️ 飞哥只给了 console 账密（minio/miniominio），**bucket 名未提供**，需在 MinIO 控制台建桶（如 flovart）并填 OSS_BUCKET；建议另建专用 access key 而非 root 凭据。
- 媒体 URL 化结论：上传/生成媒体字节落 OSS（键 {prefix}/users/{uid}/media/{key}），索引仍在 PG cloud_media；读取走 /api/me/media/{key} → BFF 307 → presigned URL，前端零改动。
- 搬迁脚本 scripts/migrate_local_media_to_oss.py（moto 实测通过）迁到服务器后对旧数据跑一遍即可，幂等可重跑。
## 拆分图层「上传失败」排查与修复（flovart-web）
- 根因链：SplitLayersPanel/MultiAnglePanel 点开对话框即自动把画布源图 POST /api/me/media → 源图超 BFF MAX_MEDIA_BYTES(50MB) → 413「单文件超过 50 MB」；前端错误体读 `json?.message` 但 FastAPI 返回 `{detail}` → 真实原因被吞，只显示兜底「上传失败」。
- 修复（前端两文件）：`components/SplitLayersPanel.tsx` + `components/MultiAnglePanel.tsx` —— ① 上传前 50MB 预检（超限直接给明确双语文案不发请求）；② 错误提取改 `json?.detail ?? json?.message`（uploadErrMsg helper）。tsc 全量检查仅存量错误，两文件干净。
- 佐证案例：wangsicong123456 用户 2026-09-21 00:42 image-gen 成功（2480x3312，base64 直传不走 media），但其拆分图层上传失败；另一候选根因 507 配额（OSS_ENFORCE_QUOTA 默认 1 + OSS_QUOTA_BYTES 默认 2GB，media_bytes_used 统计含生成产物且未剔除 deleted_at！注意 PgMeta/LocalMeta 的 SUM 均未 WHERE deleted_at IS NULL）。
- 验证命令：查该用户媒体占用 `SELECT count(*), pg_size_pretty(sum(size)::bigint) FROM cloud_media WHERE uid=<id> GROUP BY uid;` 超过 2GB=507 配额，否则多半 413 大图。
- 待拍板：50MB 上限是否放宽/前端压缩；media_bytes_used 是否应剔除 deleted_at 行。
## wangsicong123456(uid=143) 上传失败定性：507 配额
- 实测 cloud_media uid=143：count=308, used=2051MB > OSS_QUOTA_BYTES 默认 2GB → media_put 抛「云存储配额不足」→ 507。
- 已修 BFF 配额统计 bug（cloudstore.py 四处）：media_bytes_used ×2 + overview ×2，SUM 加 WHERE deleted_at IS NULL（软删 blob 已真删，不应计入）；152 passed。commit/push 飞哥自己来。
- 运维解法：服务器 .env 加 OSS_QUOTA_BYTES=10737418240（10GB）后 docker compose up -d bff；或部署修复版后软删行自动腾出额度。
## 拍板落地：50MB+前端压缩 / 配额 5GB
- 飞哥拍板：50MB 上限保持，前端自动压缩；每用户配额默认提到 5GB。
- 前端：components/workflow/media.ts 新增 compressImageIfNeeded（createImageBitmap→等比缩 4096→原格式优先/PNG 保透明→JPEG 降质 0.92/0.85→2048 兜底；gif/svg/解码失败原样返回交后端报错）；SplitLayersPanel + MultiAnglePanel 超限时先压缩再传，压缩后仍超限才报错；错误体读 json?.detail ?? json?.message。
- BFF：config.py OSS_QUOTA_BYTES 默认 2GB→5GB；compose.server.yml :-5368709120；env.server.example 注释同步。⚠️ 服务器 .env 未配 OSS_QUOTA_BYTES → compose up -d bff 即生效 5GB，无需等新镜像；deleted_at 统计修复才需要新镜像。
- 验证：BFF 152 passed；tsc 改动文件零错误。

# 2026-09-17

## @ 引用两连修（flovart-web，BFF 无改动）
飞哥需求：① 图片节点提示词输入 @，要能关联画布里**所有**素材节点（含未连线），选中后效果等同手动画节点线；
② 个人素材库文件夹「看得到打不开、里面素材选不了」。

### ② 根因（一行血案）
`MentionList.tsx` 的 folder/back 条目被标 `clickable: item.kind === 'item'` → false，
而 `pickEntry` 开头 `if (!entry.clickable) return;` 把点击整个吞掉。键盘 Enter 走同一函数同样被吞。
修：folder/back 可点击（pickEntry 本就有 kind 分支走 navigateFolder，不会误触发 Tiptap command）。
顺手补：根视图只显示文件夹 → 现在**未归档素材（folderIds 为空）也直接可选**。
分区标题「已连接」→「画布节点」。

### ① 实现：@ 候选并入画布全部素材节点 + 选中自动建线
- `references.ts` 新增两个纯函数：
  - `mergeCanvasMentionItems(connectedItems, canvasItems)`：@ 候选 = 已连线（在前，保留 chips 编号）+ 画布其余 image/video（去重追加）
  - `findUnconnectedMentionIds(ids, connectableIds, connectedIds)`：从解析出的提及里找「可引用但未连线」的 id
- `WorkflowNodePromptBar`：`canvasMentionItems = mergeCanvasMentionItems(mentionItems, referenceItems)` 传给 PromptBar；
  `handlePromptInput` 里对 missing 调新 prop `onConnectMentionIds`，并把 missing 直接并入本轮 intent ids
  （**连线是异步生效的**，不并进去这次输入的引用会被 keepConnectedMentions 过滤掉）
- `InfiniteWorkflow.handleConnectMentionIds`：批量 `connect_nodes` op（与 handleSelectWorkflowReference 同一套校验）
- 连线建立后 chips / 运行时引用 / 顺序全部自动走既有 connections 语义，零特判

### 测试
- 新增 `tests/mentionListFolderNav.test.tsx`（4 条：根视图、进/出文件夹、选素材 command、跨文件夹搜索）
  ⚠️ jsdom 没有 `scrollIntoView`，要 `Element.prototype.scrollIntoView = vi.fn()` 打桩
- 新增 `tests/mentionCanvasMerge.test.ts`（4 条纯函数回归）
- 既有 workflow/references/prompt 相关 89+ 全绿；tsc 对改动文件零报错

### 🔴 假失败血案：测试剥注释正则被字符串字面量带偏
`serviceModelScope.test.ts` 的「副标题原始名」断言突然失败：`readCode()` 的朴素剥注释
`/\*[\s\S]*?\*\//g` 把 `accept="image/*,video/*,audio/*"` 字符串里的 `/*` 当注释开头，
一路吞到下一个 `*/`，把真实的 `sub: model || '已就绪'` 整段吞掉。
修：该断言改用 `readSource`（原始源码）。**教训：静态断言的「剥注释」对含 `/*` 的字符串字面量文件不可靠**，
 断言目标行附近若有 `accept=`、URL、正则字面量等含 `/*` 的内容，直接用 readSource。

### ⚠️ 工具坑（本次会话）
Edit 工具多次「报成功但内容未落盘」（同文件反复出现），改用 python/sed 确定性写入后才稳定；
vite watcher 对脚本写入有时不触发 HMR，touch 一下即可。

### 飞哥侧手工改动（未提交，别覆盖）
`PromptBar.tsx` 今早 09:37 被手动注释了模型卡片/下拉里的「· 同步/异步」角标 span（`{/* ... */}`）。

---

## 🔴 生成产物不再覆盖素材节点（10:00 飞哥拍板）
**需求**：选中素材节点 → 调模型生成图片时，产物要落**旁边新建的节点**，不许在原节点里覆盖原素材；
体验对齐「分层 / 多角度」（那两个本来就是新建占位节点 + 连参考线）。

### 根因位置
`operations.ts::ensureWorkflowImageGenerateOperation`（唯一调用点 `App.tsx:652` 的 `handleRunWorkflowNode`）：
`sourceHasMedia` 分支做**原位替换** —— 原图复制成 `isVisible:false + isLocked` 隐藏输入节点，
原节点 id/位置被 operation 节点顶掉。用户视角 =「我的素材没了」。

### 新语义
- 原素材节点**原样保留**（可见、不持有 operation、storageKey 不动）
- 新建 operation 节点：id 用 `createId()`，落点 `x + (width||360) + 64, y 不变`（与多角度占位同款）
- 素材自动成为 `reference_image` binding（submode=image-to-image），连接 source→operation
- `selectedNodeIds` 切到新节点；`config`（模型/比例等 PromptBar 里选的）随 merge 带过去
- ⚠️ **createId 调用顺序 = 先 bindings 后 operation id**（测试里 ids 数组顺序别写反）
- 无媒体节点（普通生成节点）转 operation 仍走原位替换，未动

### 隐藏输入节点的历史包袱
旧机制产生的 `isVisible:false + isLocked` 隐藏节点存量还在画布里（workflowReferenceIntegrity.test.ts
有针对它的媒体删除保护测试）—— 新代码不再产生，但存量兼容逻辑别删。

### 测试
- `workflowOperations.test.ts` 原位替换用例重写为新建节点断言（47 passed）
- `workflowGeneration.test.ts` 图生图参考断言 `elementId` 从 hidden-input 改为原素材 id（'image-1'）
- 关联 8 文件 61 passed；tsc 零报错

---

## 🔴 「设为默认」不生效 + 芯片与列表对不上（10:06 飞哥反馈）
### 根因（两个独立问题叠加）
1. **isDefault 从未参与默认选中**：`workflowPromptPolicy.resolveWorkflowDefaultModel` 取「候选里第一个能解析出路由的」，
   完全不看 `key.isDefault` → 设置页点「设为默认」在使用侧永远无效。
2. **芯片显示的 modelId 是历史脏 id**：节点 `config.modelId` 里存着旧版写入的产品目录 id
   （如 `flovart:gemini-3-pro-image-preview`），如今候选列表是 `flovart:custom:<原始名>` 合成 id
   → 芯片解析出产品名「Gemini Pro Image (Preview)」，列表里没有任何一项与它相等（无高亮、对不上）。
   原默认填充 effect 只在 `!config.modelId` 时填，脏 id 永不纠正。

### 修复
- `resolveWorkflowDefaultModel`：① 优先取 `isDefault` 服务显式配置的模型（image→imageGenModel / video→videoGenModel），
  经 `resolveDisplayableProductModel` 归一到候选 id 空间且可路由 → 返回；② 否则回落「第一个可路由」（原行为）。
- `WorkflowNodePromptBar` 默认填充 effect：新增 `staleModelId` 判定 —— `config.modelId` **不在候选里**（候选非空）
  也视为未设置，纠正为默认模型。用户手动选的模型必来自候选，所以「不在候选」= 脏数据或已下架，纠正语义正确。
- 🎁 **顺手抓到真实产品 bug**：`productModelCatalog.IMAGE_HINT` 里的 `seed`（为 seedream 准备）会先把
  `doubao-seedance-*` 误判成 **image** 模型（IMAGE_HINT 先于 VIDEO_HINT 测试）。登记了的 seedance-2.0 没事，
  **未登记的 seedance 1.0/变体全中招**（被合成成图片模型 → 视频能力/路由全错）。已把 `seed` 收窄为 `seedream|seededit`。

### 测试
- 新增 `tests/workflowDefaultModel.test.ts` 5 条（isDefault 优先 / 不在候选回落 / 无 isDefault 原行为 / video 用 videoGenModel / stale 纠正源码断言）
- 相关 6 文件 94 passed + 回归 9 文件 63 passed；tsc 对改动文件零报错
- ⚠️ 测试 fixture 教训：`mergeSuggestedProductRouteMappings` 从 `customModels` 推路由，`videoGenModel` 必须同时出现在 `customModels` 里才能推出 video 路由

---

## 🔴🔴 素材库选择 = 直连参考图，不再新建画布节点（10:20 飞哥拍板，全天最大改动）
**需求三连**：① 点素材库文件夹 → 现有弹框**右侧悬浮**面板展示素材（不再是列表内切换）；② 选自定义文件夹素材报
「媒体文件不存在」（asset-library: 新节点渲染解析失败）；③ 选素材库图片**不要新建节点**，直接进对话框参考图区（等同手动上传参考图）。

### 关键发现：运行时早就有无节点通路，只是 UI 没接
`inputResolver.resolveWorkflowInputs` 的 `addMention`：mention 带 `assetId` 且 `options.assets` 有该素材 →
`assetResource()` 直接构造 asset locator 资源，**无需任何画布节点**。生成主链路 `workflowGeneration.ts:239` 已传
`assets: runtime.assets`，`App.tsx:671` 已把素材库全量注入。**缺的只是 UI 层还在走「新建节点 + 建线」老路**。

### 改动
| 文件 | 改动 |
|---|---|
| `RichPromptEditor.tsx` | @ 弹层素材分支**直插 mediaMention**（attrs.assetId），不再调 onSelectAsset 新建节点；Handle 新增 `insertMention`/`removeMention`；删 onSelectAsset prop |
| `PromptBar.tsx` | `handleAttachAsset`：+号选择器选素材 → 直插 mention；chip X 对 `asset:` 前缀走 `removeMention`（从文档删 mention），连线 chip 走原 onDisconnectReference |
| `WorkflowNodePromptBar.tsx` | `keepConnectedMentions` 放行 `asset:` id（不按 connections 过滤）；chips 合并 `buildDirectAssetChips`（asset mention → chip，thumbnail 优先素材库 dataUrl） |
| `InfiniteWorkflow.tsx` | 删 handleSelectAsset（建节点路径）与 onSelectAsset 接线 |
| `promptPaste.ts` | 粘贴 asset mention 不再重建节点，原样保留（ops=[]） |
| `MentionList.tsx` | 文件夹点击 → **fixed 右侧悬浮面板**（子文件夹可下钻 + ＜返回 + 点素材即选中）；删列表内导航/back 条目 |
| `MediaMentionExtension.tsx` | mention 图标统一 16×16（对齐修复）；img onerror 降级为图标（失效 blob 不再塌陷）；`asset-library:` 协议异步解析真实缩略图 |
| `references.ts` | 新增纯函数 `buildDirectAssetChips` |

### 测试
- `mentionListFolderNav.test.tsx` 重写为悬浮面板语义（5 条）；`mentionCanvasMerge` +3（chips 合并）；`workflowPromptPaste` 重建节点用例改「原样保留、ops=[]」
- 全量回归 18 文件 **181 passed**；改动文件 tsc 零报错
- ⚠️ python 批量替换时 `});\n});` 尾锚截断把 describe 闭合搞坏过一次（no tests）—— 批量改测试文件后必须先看尾部结构

### 遗留
- 存量「素材引用节点」（图片4 那种，显示媒体文件不存在）不再产生，旧的手动删即可
- 画布生成产物节点（storageKey 型）的内联 mention 缩略图：失效 blob 已降级图标，真实图需要异步解析 —— 已支持 asset-library: 协议，storageKey 型未做

## 模型选择面板三连修（flovart-web，飞哥 09-17 上午反馈）
「默认模型在列表没有选中态；而且没法切换选中其他模型了；列表按家族分组（gemini 3 个 / gpt 3 个）做好看」。

**根因（一个身份不一致，两个症状）**：PromptBar 图片模型面板（image 分支，apiConfigs 服务维度列表）的列表项身份是**网关原始模型名**，而 `activeModel`（= 节点 config.modelId）与候选（dynamicModelOptions.image）全是**产品 id**（flovart:...）：
1. `selected = activeModel === model` 永远 false → 默认模型永不高亮；
2. 点击把原始名写入 config.modelId → WorkflowNodePromptBar 的 stale 纠正 effect（09-16 加的）判定「不在候选」→ 立刻回写默认 → 切换被吞。

**修法**：列表项选择身份统一 `resolveDisplayableProductModel(raw).id`（selected 比较 + changeActiveModel 入参 + productId 去重）；分组用新增导出 `promptBarPolicy.modelFamilyLabel()`（与 productFamily 关键词同口径，关键词 includes 对原始名/产品 id 同样成立），组头 = 家族名 + 数量徽标 + 细分隔线，保持首现顺序。

- 改动：`components/PromptBar.tsx`（image 分支重写渲染）、`services/promptBarPolicy.ts`（+modelFamilyLabel）。
- 新测试：`tests/promptModelPanel.test.tsx`（5 条：家族归类、选中高亮、点击上报产品 id、分组渲染）。
- 存量遗留顺手修：`tests/promptBarUpload.test.tsx` keyframe 用例点芯片按 `seedance-2.0` 原始名找（上轮 promptModelLabel 改口径后 /Seedance/ 已匹配不到，属漏改的第 4 处调用点——不过是测试不是源码）。
- ⚠️ 本会话 Bash 工具 `cd /d/code/flovart-web && ...` 的 cd 前缀会被莫名剥离 → 用 node `-e` 里 `process.chdir` + `spawnSync(vitest)` 绕过；`workflowDefaultModel.test.ts` 有 `readFileSync` 相对路径断言，cwd 不对会假失败。
- 验证：6 文件 24 passed；tsc 对改动文件零报错；web 37522 / BFF 8300 均 200。

## 「图片 1 mention 图标塌陷没解决」续修（flovart-web）
用户反馈上轮修复无效。放大截图确认：塌陷的不是「坏 img」，是 **emoji 兜底图标本身** ——
画布上传/生成节点 `metadata.href` 为 undefined（字节按 `storageKey` 存 workflowMediaStorage），
mention attrs 里的 thumbnail 恒为空 → 永远走 emoji 分支；「多角度」operation 节点带 dataUrl 所以正常。

**修法（mention 链路补 storageKey 通路）**：
- `references.ts::toWorkflowMentionItems` / `PromptBar` tier2 映射 / `MentionList.MentionItem` / `promptReferenceClipboard.PromptReferenceMention`：补 `storageKey` 字段；
- `MediaMentionExtension`：加 `storageKey` attr；导出 `registerMentionThumbnailResolver`（模块级注册表，mention id → storageKey，供历史文档旧 mention 反查）；NodeView 无可直显 thumbnail 时按 storageKey 调 `loadWorkflowMediaBlob` 换真图，失败保持 16×16 图标；
- `RichPromptEditor`：挂载时注册解析器（读 referenceItemsRef）；两条 insertContent 路径 attrs 补 storageKey。
- ⚠️ 血案：NodeView **构造期** icon 尚未挂进 DOM，`icon.isConnected` 恒 false —— 首次调用写在构造期的重试函数若开头就判 isConnected 会永远直接 return。判活只能在 setTimeout 回调和升级回调里做。
- 新测试 `tests/mediaMentionThumbnail.test.tsx`（3 条，vi.mock workflow/media）；7 文件 29 passed；tsc 改动文件零报错。
- 工具坑补充：`git -C /d/code/flovart-web` 报 No such file，改 `git -C "D:/code/flovart-web"`（Windows 盘符形式）才认。

## 素材库选图不出「带 X/序号的参考图 chip」根因（flovart-web，同日第三发）
MentionList 里素材条目的 mention **id 用了素材裸 id（`id: a.id`）**，而整条 chip 链路
（buildDirectAssetChips 合成、chip X 的 removeMention、keepConnectedMentions 放行）都认
**`asset:<id>` 前缀**（PromptBar tier3 / handleAttachAsset 均带前缀）→ @ 弹层/悬浮面板选的
素材永远合成不出 chip。修：MentionList 三处（搜索结果、根视图未归档、悬浮面板）id 改为
`asset:${a.id}`；mentionListFolderNav 测试补 `picked.id === 'asset:a_inroot'` 断言。
教训：**mention id 的前缀协议（asset:/shared:）是跨组件契约，新增入口必须对齐**。

## 素材库悬浮面板「飞走」+「点多次没反应」（flovart-web）
MentionList `openPeek` 原先用**整个列表容器**的 rect 定位（containerRef.getBoundingClientRect().right/top），
实际渲染时与可见弹框对不上（截图中面板出现在视口右下角远离弹框处）。修法：
- **锚定触发元素**：文件夹行 click 传 `e.currentTarget`/`data-index` 查询按钮的 rect；悬浮面板内「＜返回」锚面板自身（`peekPanelRef`）、子文件夹锚自身行；
- **视口 clamp**：右侧放不下放锚左侧，再不行贴视口右缘；top/maxHeight 上下都不出视口；
- **toggle**：再点同一个文件夹 = 关闭面板（pickEntry 与子文件夹按钮都做了），点不同文件夹 = 重新锚定打开 —— 治「点多次没反应」的观感。
教训：fixed 定位的面板锚点绝不能拿「祖先容器」rect（Portal/隐藏实例会飘），永远锚**触发事件的那个元素**。

## Gemini 芯片显示名不带版本号（flovart-web）
用户发现：选中 gemini 模型芯片显示「Gemini Flash Lite Image」（无 3.1），gpt 却显示「GPT Image 2.5 Sunburst」（带 2.5）。
不是过滤逻辑，是**产品目录命名口径不统一**：GPT 系列的 name 天生带版本号，Gemini 系列全没带。
芯片/列表显示名都经 resolveDisplayableProductModel → 目录 name。
修：`tools/flovart/product-models.js` 5 条 Gemini 条目补版本数字
（Gemini 3 Pro Image / Gemini 3 Pro Image (Preview) / Gemini 3.1 Flash Image / Gemini 3.1 Flash Lite Image / Gemini 3.1 Flash Image 2K）；
`tests/serviceModelScope.test.ts:235` 断言同步更新。5 文件 72 passed。
遗留：veo 条目（flovart:veo-3.1 name 只有「Veo」）同样口径问题，用户没提先不动。
教训：**产品目录 name 是唯一显示名来源，新增条目 name 必须带版本数字**，否则芯片与原始名对不上（老问题第 N 次变体）。

## 悬浮面板「飞走」续修第二发：Portal 到 body（flovart-web）
锚定触发元素后仍然飞 → 真根因是**CSS transform 祖先污染 position:fixed**：
@ 弹框挂在 Tiptap suggestion 弹层里，该弹层（或画布容器）带 transform/scale →
fixed 不再相对视口，而是相对该祖先，getBoundingClientRect 的视口坐标全偏。
修：`MentionList.tsx` peek 面板用 `createPortal(..., document.body)` 渲染，彻底逃出 transform 上下文。
（jsdom 里 testing-library screen 查询 document.body，Portal 不破坏断言，12 passed。）
教训沉淀：**fixed 定位元素若祖先可能带 transform，一律 Portal 到 body**，锚点计算才可信。

## 拖图到对话框松手却建节点（flovart-web）
PromptBar 显示「松手上传参考媒体」但松手后画布建了新节点。根因：画布根 `onDropCapture={dropMedia}`
**捕获阶段**先于 PromptBar 自己的 onDrop 执行，且 dropMedia `stopPropagation()` 吞事件 →
PromptBar 的 drop handler 永远收不到（dragEnter/Over 不受影响，所以遮罩正常显示——典型假象）。
修三处：
- `InfiniteWorkflow.dropMedia`：drop target 在 `[data-promptbar-shell]` 内 → 直接 return 让路；
- `PromptBar` 壳 div 加 `data-promptbar-shell` 标记；
- `PromptBar` onDrop 新增认 `STUDIO_MEDIA_DRAG_TYPE`（素材库条目 dragStart 的 JSON payload）→
  `handleAttachAsset(payload.id)` 插 `asset:` 引用 chip；找不到素材再回落文件上传通路。
  dragEnter/dragOver 同步放行该类型（素材库拖拽也能显示「松手上传参考媒体」遮罩）。
教训：**画布级 onDropCapture 与浮层控件的 drop 共存时，捕获 handler 必须先判断落点**；
`dataTransfer.types` 里 Files 拖拽（item.type=image/*）与自定义类型要分开判定。
验证：6 文件 23 passed；tsc 改动文件零报错。

## 引入参考图 → 自动切「图生图」（flovart-web）
PromptBar 新增 effect：image 模式下，图片类参考 chips 数量 0→N 跳变且当前子模式为 text-to-image
且模型支持 image-to-image → 自动 onGenerationSubmodeChange('image-to-image')；chips 清零且当前
image-to-image → 自动切回 text-to-image。只统计 elementType==='image' 的 chip（视频 chip 不触发）；
只在数量跳变时触发 → 参考图存在期间用户手动切回文生图不会被顶回。
新增 tests/promptModeAutoSwitch.test.tsx（3 条：出现/清零/视频chip不触发）。4 文件 14 passed。

## flovart-web 前端出包（09-17 中午，355187d）
- 产物：`flovart-web-dist-355187d-20260917.zip`（4.62 MB / 65 条目 / CRC 全过 / 无多余顶层目录）
- 入口 bundle：`index-DxTi8nFZ.js`（1.62 MB，gzip 512KB）+ vendor-react/tiptap/genai 分包
- 体检：sourcemap 0 泄漏；localhost 硬编码 4 条与旧包逐字一致（既有行为，非本次引入）；
  本次改动确认进产物（`data-promptbar-shell` / image-to-image 在 WorkflowWorkspace-DLhOlJjq.js）
- 流程执行：旧 dist `mv` 挪到 `/d/code/_flovart-web_prev_20260917-124515`（绕 safe-delete bulk 闸门），
  HashRouter + base './' → 同一份包任意域名/子路径可用
- ⚠️ 既有问题：WorkflowWorkspace chunk 914KB、index 1.62MB 超 500KB 警告（非本次引入，待分包优化）

## 普通用户默认模型不跟管理员对齐 + 使用侧只认已发布清单（flovart-web，09-17 下午）
飞哥澄清：平台服务管理 UI 保持隐藏不要恢复；管理员侧显示正常，问题只在普通用户。拍板新规：
**使用侧（管理员和普通用户一视同仁）只认「管理员当前发布到平台」的 AI 服务；每次打开模型面板
实时拉 /api/platform/services 对齐；发布后直接删除（未走下架）的服务对所有人立即不可见。**

根因两个：
1. App.tsx 平台 Key 注入时 `imageModel = models.find(第一个图片模型)`（网关目录序）→ 普通用户
   默认 = gpt-image-1（列表里根本没有）；管理员设的默认在他们**本地 keyVault** 的 isDefault 条目上，
   不会传播 —— 真正能传播的是**发布时带走的 service.imageGenModel**。
2. 候选白名单 `allowed = 网关目录 ∪ 发布清单` → 网关有、未发布的模型（_2k 定档等）漏进普通用户候选。

修法（App.tsx + PromptBar.tsx + platformServiceModelList.test.ts 注释）：
- **注入默认**：先 loadPlatformServices，`imageGenModel` 优先级 = ① 服务显式 imageGenModel/videoGenModel
  → ② 服务模型清单第一个可识别图/视频 → ③ 网关目录第一个（兜底）；顺带把 loadPlatformServices
  提前、复用变量（原来在 payload 构造后又拉一次）。已有用户靠 needsUpdate 的 imageGenModel 对比自动刷新。
- **候选白名单**：`allowed = sharedModels`（不再并 hostedModels）；空清单时
  filterProductModelsByPlatform 自带回退防空白。文本模型不过滤（memo 只动 image/video）。
- **面板条目过滤**：image 分支 items 加 `.filter(productId ∈ imageModelOptions)`（候选空时不过滤），
  管理员本地已删服务、用户本地影子条目未重铺时也立即隐藏。
- **实时对齐**：PromptBar.togglePanel 打开 model 面板时派发 `flovart:platform-services-resync`
  CustomEvent；App 监听 → loadPlatformServices（只更新 store，候选即时重算；影子条目重铺仍归
  3 分钟/聚焦的 resync effect）。
- ⚠️ 命名空间教训：CustomEvent 用 `flovart:` 前缀避免与画布事件冲突。
- ⚠️ platformSharedService.test.tsx 6 个失败为**存量**（飞哥手动注释平台服务管理 DOM 所致），
  stash 验证过与本次无关。
- 验证：tsc 改动文件零报错；platform/model 相关 9 文件 61 passed。
- 待办：测试环境要用新包重新出包部署（355187d 是旧逻辑）。

## 平台服务管理 CLI（flovart-web/scripts/platform_services_admin.mjs，09-17）
背景：管理 UI 已隐藏，但测试环境已发布清单里留着不想要的模型（_2k），需要无 UI 入口修剪清单。
- `list`：登录 BFF 打印全部服务（models/imageGenModel/videoGenModel/defaultModel/suspended）+ revision；
- `remove <子串>`：从各服务 models/customModels 删匹配模型，指向字段（imageGen/videoGen/default）命中一并清空；
  整条删空则跳过提示（服务至少留一个模型）；默认 dry-run，`--yes` 才 PUT（带 base_revision 乐观锁）。
- 连接：`--base/--user/--password` 或 env `BFF_BASE`/`NEWAPI_ADMIN_USERNAME`/`NEWAPI_ADMIN_PASSWORD`。
- ⚠️ BFF 会话 Cookie 是 **bff_session + bff_uid 两个**（不是 fv_session），Node 侧用 `res.headers.getSetCookie()` 解析。
- ⚠️ `VAR=$(...) && node ...` 里 shell 变量能展开进参数（"$USER"），但 `node -e` 里要小心 env 与参数的区别。
- 判定口径：新代码只藏「不在已发布清单」的模型；清单里真有 `_2k` 就得用这个脚本删（或恢复管理 UI）。

## 工作流内参数粘性（flovart-web，09-17 下午第二发）
飞哥需求：同一工作流里第一次选好模型/比例/画质尺寸后，后面再生图默认相同选项，直到手动切换。
- 新模块 `components/workflow/stickyWorkflowConfig.ts`：模块级粘性参数，**image/video 分桶**（图片 1:1 不污染视频 16:9），keyframe 并入 video 桶；只记 5 个键（modelId/aspectRatio/quality/resolution/durationSec）；会话级生命周期（刷新即清空，不持久化——避免跨天旧参数覆盖管理员新默认）。
- `WorkflowNodePromptBar.patchConfig(patch, {remember?})`：默认 remember:true 记录用户手动选择；**fill effect（默认模型纠正/新节点补默认）必须传 remember:false**，否则全局默认被固化成"用户选择"、管理员改默认后新节点永远不跟随。remember 用 normalize 后的 patch（换模型时 quality 被夹回合法档记的是夹后值）。
- fill effect：新节点（config.modelId 空）优先取粘性模型（需在当前候选里），否则回落 defaultMappedModelId；stale 纠正仍回默认。
- 播种 effect（挂载一次，seededRef）：仅**从未生成过**（status undefined/'idle'）且 config 里对应字段为空的节点，补粘性比例/画质/分辨率；已生成过的节点不动（防用户回看历史节点参数被悄悄改）。
- 测试 `tests/workflowStickyConfig.test.tsx`（4 条）。⚠️ 存量发现：`ratioPatchRoundtrip` 失败（stash 验证非本次）——根因是「生成参数」按钮在 activeProductModel 未解析时改开模型面板（PromptBar.tsx:1597 三目），而旧 harness 的 onChange 是纯 mock、config.modelId 不回流 → 永远解析不出。修：测试加 **StatefulPromptBar 有状态 harness**（onChange patch 回流 node，与 applyOps 一致）。
- 验证：粘性+比值+workflow 相关 4 文件 17 passed；tsc 改动文件零报错；web 200。

## 测试环境平台服务清单实况（09-17）
用 platform_services_admin.mjs 连 https://flovart.oneapis.cn 实测：**已发布平台服务 = 0 条**（登录成功，凭据 newapi-bff 有效）。
推论：①「莫名的 _2k 模型」来自**网关目录渠道模型**，不在发布清单里，remove 无从删起——
要么去 new-api 控制台把该模型从渠道下掉（旧包立即生效），要么发新包后由管理员真正「发布服务」收窄；
② 新代码空清单时回退全量目录（防空白）→ 测试环境不发布服务的话过滤永远不生效。
另：飞哥本机 CMD 跑脚本报 MODULE_NOT_FOUND = `\` 换行 CMD 不认 + 目录不对，要单行命令全路径。

## 15:30 �޸����½��ڵ����û����ճ�����á���flovart-web��
- �������ֶ���ò������½��ڵ������ص�Ĭ�ϡ�
- ����InfiniteWorkflow ��Ⱦѡ�нڵ㸡�� WorkflowNodePromptBar��~2365 �У�**���� key**���л�ѡ�нڵ�ʱ React ����ͬһʵ�������� effect �á�ÿʵ��һ�Ρ��� seededStickyRef���е��½ڵ� B ʱ ref �� true �� B ��Զ������ճ�Ա���/���ʡ��ɲ��� unmount ���ع����Բⲻ������
- �޸���WorkflowNodePromptBar ���� effect ��Ϊ�� node.id ���أ�seededNodeIdRef��deps [node.id]����ʵ������/�� key ������Ⱦ��ʽ����ȷ��
- �ع���ԣ�workflowStickyConfig.test.tsx ���� SwitchingPromptBar harness�������� key ʵ�����ã���5 passed����� workflow ������ͨ����workflowNodeOverlays 2 ʧ��Ϊ������stash ��֤�뱾���޹أ���tsc �Ķ��ļ��㱨����

## 16:05 �޸���ͼƬ�ڵ�ߴ粻ճ�ԡ��½ڵ���� 1K����flovart-web��
- ���� 1��ͼƬ�ġ��ߴ硹оƬ��1K/2K������Ƶ���� config.resolution���� onVideoResolutionChange���������� effect ֻ�� video ģʽ�� resolution �� ͼƬ�½ڵ���Զ��Ĭ�� 1K��
- ���� 2�������Σ���PromptBar �� sanitize effect ��ѡ�ģ�Ͳ�֧�ֵ� resolution���Զ��л�Ĭ�ϣ��� 720p��1K������ onVideoResolutionChange �� patchConfig remember:true �� **��ճ�Գߴ縲�ǳ� 1K**����Ⱦ���������½ڵ㡣
- �޸����ٲ��� effect �� image Ҳ�� resolution����sanitize �Զ������� {auto:true} ��ǣ�WorkflowNodePromptBar �յ� auto ʱ remember:false��durationSec ͬ������������ճ��ֻ���û��ֶ�ѡ��
- ���ԣ�workflowStickyConfig 5 passed���ع������չΪ���� resolution ���� + ճ�Բ��� sanitize ��Ⱦ����tsc �Ķ��ļ��㱨����
- ?? �ȿӣ����� Edit �����ɹ���δ���̣�PromptBar �����С�seed �У����� tsc/����ץ�أ��ĺ����ض���֤��
## 16:20 ǰ�˴��������flovart-web @ a058a52���������ɾ���
- ���flovart-web-dist-a058a52-20260917.zip��4.62MB / 65 �� / CRC ȫ�� / �� dist/ ǰ׺��
- ���ê�㣺assets/index-DKVfdj9L.js��index.html �� ./assets ���·����HashRouter �� ��������/��·��ͨ�ã�
- ��죺sourcemap 0 й©��localhost Ӳ������ɰ�����һ�£�������Ϊ�����Ķ�ê�� flovart:platform-services-resync �� index-DKVfdj9L.js + WorkflowWorkspace-CNgtTuZ6.js
- �� dist Ų�� D:\code\_flovart-web_prev_20260917-161711 �ɻع�

## 17:35 澄清测试环境「0 条服务」误判 + 删掉 _2k（flovart-web 脚本修复）
- ⭐ 血案：`platform_services_admin.mjs` 一直没解 BFF `ok()` 的 `{success,data:{...}}` 信封，直接读 `body.items` → undefined → 误判「共 0 条服务」「revision=undefined」。实际测试环境 docker 卷 `/var/lib/docker/volumes/flovart-bff-data/_data/flovart_cloud.db` 里一直有 6 条服务（revision 16）。**任何读 BFF 接口的脚本都必须 `body.data || body` 解包**。
- 测试环境 BFF 是 SQLite 兜底（POSTGRES_DSN 空 → USE_PG=False），库在容器 /data/flovart_cloud.db（2.54GB，媒体+日志大头，正常）。
- 已执行：脚本 remove "gemini-3.1-flash-image-preview_2k" --yes → 6 条服务 models 全部剔除该模型 + 1 条服务的 imageGenModel 指向被清空，revision 16→17，newly_revoked 含 _2k。验证 residual=0。nano-banana-pro_2k / doubao-seedream-5-0-pro-260628_2k 等未误伤。
- 脚本顺带修好：apiGet/apiPut 解包 data 信封。

## 17:50 恢复设置面板「平台服务」管理 UI（flovart-web）
- 飞哥拍板选 A（恢复管理 UI）。分两段：
  ① 09-16 版「平台服务（所有账号可用）」服务端清单区块（下架/重新上架/删除）——原本只是被注释（1845-1926），反注释即可。
  ② 09-14 版「平台服务」影子卡片区（platformSourceKeys 只读卡片 + 管理员内联编辑表单：展示名称/模型清单/默认生图/默认视频/支持能力/保存到平台）——09-15/16 被整块删除（⛔注释 1640-1647 处只留说明），从 git 历史 727c6a0 提取 1514-1693 行重新插入。所有依赖函数（platformDraftListed/beginEditPlatform/savePlatformDraft/publishingId 等）当时都保留着，直接可用。
- 结果：platformSharedService.test.tsx 13/13 全过（6 个存量失败转绿）；SettingsPanel.tsx tsc 零报错。
- ⚠️ 踩坑：Git Bash sed /r 锚点含中文括号时匹配要小心（`）。` vs `。 `），锚点选短且唯一的中文短语。

## 18:05 按飞哥新拍板重做设置页平台服务 UI（flovart-web）
- 反馈：恢复的 09-14「平台服务」影子卡片区不是想要的 —— 「已链接/管理员配置/模型 741 个」假卡片倾倒网关全量模型名，不是项目精选。
- 最终语义（三次演进：09-14 影子卡片 → 09-16 服务端清单区 → **09-17 影子卡片彻底不渲染**）：
  - platformSource 影子条目在设置页【不渲染任何卡片】（删除 09-14 版区块，本次是删不是注释；注入逻辑保留，模型选择器链路不受影响）。
  - 管理员维护平台服务 = 自己的 AI 服务卡片（发布到平台/下架/重新上架/设为默认/编辑/删除）+「平台服务（所有账号可用）」服务端清单区。
  - 服务端清单卡片不再倾倒模型名（原 listed.slice(0,5) dump），改显「默认生图：X · 默认视频：Y · N 个模型」。
- 测试：platformSharedService.test.tsx 按新语义重写为 9 条（影子不渲染/700+ 不倾倒/空态引导/BYOK 正常/管理员才有发布按钮/Key 池不渲染 + sameService 与 _gateway 源码契约），9/9 过；相关 3 套 11 passed；SettingsPanel tsc 零报错。
- ⚠️ 坑：①Edit 再次报成功未落盘（空态文案），靠 grep 抓回重改——改动后必须回读验证；②测试断言「平台模型」撞了 hosted 普通用户的固定 Tab 名（tabList key='platform' label='平台模型'），测试数据 key 名避开即可。

## 18:25 严格截图 UI + 「没发布不可见不可用」完整落地（flovart-web）
- 飞哥拍板：AI 服务 tab 严格只展现「+添加AI服务」保存的卡片；管理员发布→全员可用；未发布/下架→普通用户不可见不可用；人人可自配 BYOK（普通用户无发布按钮）。
- UI：①删除「平台服务（所有账号可用）」服务端清单区（第二次删，这次是彻底删）；②删掉残留的 platformSourceKeys memo（不再渲染任何影子卡片）。孤儿服务（本机卡片已删、服务端条目还在）→ 只能走 scripts/platform_services_admin.mjs 清理。
- 候选链路三处收紧（这是「700+ 漏进选择器」的真正源头）：
  ① hooks/useApiKeys.ts：抽出 modelOptionsFromKeys(keys, {allowCatalogFallback})；allowCatalogFallback=false 时候选空就是空，不回退产品全目录。
  ② utils/platformModelGate.ts：filterProductModelsByPlatform 加 {strict} 选项——空平台数据/空交集 → 返回 []，不回退。
  ③ App.tsx platformFilteredModelOptions 重写：候选 = own（非 flovart_platform 非 platformSource 的 key，allowCatalogFallback:false）∪ shared（影子条目，按服务端最新清单 strict 过滤防 resync 滞后）。**平台 Key 池的网关全量清单从此不进候选**（它只是凭据）。deps 换成 [hostedAuthed, hostedPlatformServices, userApiKeys, dynamicModelOptions]。
- 测试：platformServiceModelList 契约更新 + 新增 2 条源码契约（modelOptionsFromKeys 严格开关、gate strict）；platformSharedService 新增「服务端清单区已删」断言（⚠️ not.toMatch(/平台服务（所有账号可用）/) 会命中注释，要用 />...</ 精确匹配 JSX 文本节点）。4 文件 23 passed；改动文件 tsc 零报错。
- 数据侧提醒：管理员卡片本身的 customModels 若是当年网关全量同步的 741 条，发布前先在「编辑」里清成精选清单，否则发布快照仍会带 741（选择器里显示的是可识别产品模型，会偏多）。

## 18:55 清空本地 BFF 全部已发布 AI 服务
- 飞哥要求清空本地管理员配置的服务。`platform_services_admin.mjs wipe --yes`（凭据从 D:/code/flovart-bff/.env 的 NEWAPI_ADMIN_USERNAME/PASSWORD 读）。
- 清空前 4 条（含 500+ 模型的「OpenAI」），revision 8→9，现 0 条。备份：`D:\code\flovart-web\platform-services-backup-2026-09-17T10-56-46.json`（ISO 时间戳是 UTC，别被 +8 搞混）。
- 撤回集已记录 published_models → 前端 strict 闸门兜底；打开模型面板会派发 resync 实时对齐。

## 19:27 前端出包第二发 + 测试环境 _2k 手删
- 出包：`flovart-web-dist-a058a52-dirty-20260917-2.zip`（4.62MB / 65 条 / CRC 全过 / 无 dist/ 前缀）。入口锚点 **index-CR4YxSan.js**；resync 锚点在 index-CR4YxSan.js + WorkflowWorkspace-Dgcas_6l.js。旧 dist 挪至 D:\code\_flovart-web_prev_20260917-192620。
- ⚠️ 关键发现：**测试环境 API（flovart.oneapis.cn，revision=17，无 _2k）与飞哥 docker exec 查的容器 SQLite（revision=48，imageGenModel 有 _2k）读的不是同一份数据**——域名路由疑似指向另一个实例（蓝绿残留）。脚本 remove 报「无匹配」就是这原因，待飞哥手删后验证。
- 给飞哥的三条 docker exec 命令（备份 payload 行 → json 剔除 _2k + revision+1 → 复查），imageGenModel 原本就指向 _2k 的服务删除后字段清空显示 None=正常。

## 20:20 🔴🔴 「下架/上架/发布解耦到模型级」+「AI 服务配置云端化」（飞哥最终拍板）
### 根因定性（飞哥怀疑「三个模型识别 id 相同」→ 实为**服务身份撞车**）
`sameService` 只比 provider+name+models → 三张只有默认模型不同的卡（gpt-image-2 / sunburst / flare）全部匹配到服务端同一条 → 点任何一张「下架」=下架同一条；普通用户只见第一条快照的模型。
- 测试环境实况（19:43）：服务端仅 1 条服务 models=[gpt-image-2,gpt-image-1.5,gpt-image-1]，而 3 张卡都显示「平台已发布」= sameService 误匹配实锤。

### ① BFF：suspendedModels 模型级下架
- `platform_catalog.py`：+`service_suspended_set()`；`published_models()` 排除模型级下架；`suspended_models()` 并入（409 文案 reason=suspended 带服务名）。
- `routers/platform_services.py`：白名单 +`suspendedModels`；**⚠️ 勿放进 _LIST_FIELDS**（非列表会被先强转 []，掩盖脏数据）——专属兜底：非列表=剔除键、空串剔除、去重保序。
- 闸门零新增代码：模型掉出 published → merge_revoked 自动进撤回集 → 409；恢复自愈。
- 测试：test_platform_suspension.py +4（全量 73 passed）。

### ② 前端：发布/下架/上架全部改模型勾选弹窗（SettingsPanel）
- 发布=勾选要发布的模型（默认全勾）；下架=勾选要下架的（默认全不勾）→ suspendedModels；重新上架=勾选要恢复的（默认全勾）→ 移出 suspendedModels + 清 suspended。存量服务级 suspended 仍被 BFF 支持，重新上架时一并清。
- sameService 收紧：+imageGenModel/videoGenModel/defaultModel（卡片身份不再互相顶替）。
- 卡片 chip：「平台已发布 · N 个模型已下架」。
- App.tsx allowed 白名单扣除 suspendedModels（小写归一，与 platformModelGate.norm 一致）。
- 验证：tsc 改动文件零报错；platform 相关 17 passed。

### ③ 接口1：AI 服务配置云端同步（新能力）
- BFF `keys.py` 新增 `GET/PUT /api/user/ai-services`（require_session，按 uid 存 cloudstore doc scope=user_keys doc_key=ai_services）；**服务端 AES-GCM 加密落库**（encrypt_secret，DB 泄漏无明文）；上限 200 条 / 512KB。
- 前端 `useApiKeys.ts`：登录+storageAligned 后 GET 对齐（服务器有→采纳 own keys、保留本地派生条目；服务器空本地有→首推）；此后变更防抖 1.5s 整体 PUT（LWW）。推送**剔除派生条目**（platformSource/flovart_platform）。
- 本地 BFF 已重启（8300），新接口未登录 401 验证过。
